# SRE Ops Checks Tracking

## Monthly Check Last Run Dates

ci_status: 2026-08-06
info_endpoint_quality: 2026-08-06
external_deps: 2026-08-06

## Container Log Review History

lucos_schedule_tracker: 2026-07-14
lucos_media_weightings: 2026-07-13
lucos_photos_worker: 2026-08-02
lucos_arachne_explore: 2026-08-02
lucos_arachne_web: 2026-08-08
lucos_backups: 2026-08-08
lucos_repos_app: 2026-08-08
lucos_dns_bind: 2026-07-27
lucos_loganne: 2026-07-23
lucos_configy: 2026-07-23
lucos_contacts_app: 2026-08-08
lucos_contacts_db: 2026-08-06
lucos_contacts_googlesync_import: 2026-07-05
lucos_contacts_web: 2026-07-05
lucos_creds: 2026-07-14
lucos_creds_configy_sync: 2026-07-05
lucos_creds_ui: 2026-07-05
lucos_dns_sync: 2026-07-09
lucos_eolas_app: 2026-08-08
lucos_eolas_db: 2026-08-06
lucos_eolas_web: 2026-07-09
lucos_locations_mosquitto: 2026-07-19
lucos_locations_otfrontend: 2026-07-23
lucos_locations_otrecorder: 2026-07-27
lucos_locations_oauth2_proxy: 2026-08-06
lucos_mail_smtp: 2026-08-02
lucos_photos_api: 2026-07-19
lucos_arachne_ingestor: 2026-07-15
lucos_arachne_search: 2026-07-15
lucos_arachne_triplestore: 2026-07-13
lucos_mail_docs: 2026-08-02
lucos_photos_postgres: 2026-07-27
lucos_photos_redis: 2026-07-18
lucos_scenes: 2026-08-06
lukeblaney_co_uk: 2026-08-06
lucos_media_manager: 2026-07-14
lucos_media_metadata_api: 2026-07-23
lucos_monitoring: 2026-07-14
lucos_media_seinn: 2026-08-02
tfluke: 2026-08-02
lucos_media_metadata_api_exporter: 2026-07-15
lucos_media_metadata_manager: 2026-07-15
lucos_notes: 2026-07-15
lucos_root_app: 2026-07-19
lucos_router: 2026-07-14
semweb: 2026-08-02
lucos_time: 2026-08-08
lucos_aithne: 2026-07-31
lucos_arachne_mcp: 2026-07-19
lukeblaney_blog: 2026-07-23
lucos_docker_health_app: 2026-07-18

lucos_docker_mirror_web: 2026-07-13
lucos_docker_mirror_registry: 2026-07-09
lucos_worlds_web: 2026-07-09
lucos_worlds_db: 2026-07-31
lucos_docker_mirror_info: 2026-07-13
lucos_firewall: 2026-08-02

## SSH Hostname Note

Always use `avalon.s.l42.eu` (not the alias `avalon`) for SSH. The SSH config uses `*.s.l42.eu` pattern. `ssh avalon` fails with host key verification error.

## Notes

- ops-checks.md was previously corrupted (null bytes). Rewritten 2026-03-06.
- Container names corrected 2026-04-02: authentication→lucos_authentication, bind→lucos_dns_bind, loganne→lucos_loganne, media_manager→lucos_media_manager, media_metadata_api→lucos_media_metadata_api, media_metadata_api_exporter→lucos_media_metadata_api_exporter, media_metadata_manager→lucos_media_metadata_manager, monitoring→lucos_monitoring, notes→lucos_notes, root→lucos_root, router→lucos_router, seinn→lucos_media_seinn, time→lucos_time, lukeblaney.co.uk→lukeblaney_co_uk. New container lukeblaney_blog added.
- Container list as of 2026-05-25 (avalon, 52 containers): lucos_arachne_explore, lucos_arachne_ingestor, lucos_arachne_mcp, lucos_arachne_search, lucos_arachne_triplestore, lucos_arachne_web, lucos_authentication, lucos_backups, lucos_configy, lucos_contacts_app, lucos_contacts_db, lucos_contacts_googlesync_import, lucos_contacts_web, lucos_creds, lucos_creds_configy_sync, lucos_creds_ui, lucos_dns_bind, lucos_dns_sync, lucos_docker_health_app, lucos_docker_mirror_info, lucos_docker_mirror_registry, lucos_docker_mirror_web, lucos_eolas_app, lucos_eolas_db, lucos_eolas_web, lucos_locations_mosquitto, lucos_locations_otfrontend, lucos_locations_otrecorder, lucos_loganne, lucos_mail_docs, lucos_mail_smtp, lucos_media_manager, lucos_media_metadata_api, lucos_media_metadata_api_exporter, lucos_media_metadata_manager, lucos_media_seinn, lucos_media_weightings, lucos_monitoring, lucos_notes, lucos_photos_api, lucos_photos_postgres, lucos_photos_redis, lucos_photos_worker, lucos_repos_app, lucos_root, lucos_router, lucos_scenes, lucos_schedule_tracker, lucos_time, lukeblaney_blog, lukeblaney_co_uk, semweb, tfluke
- 2026-05-25: lucos_comhra_agent and lucos_comhra_llm decommissioned (no longer present on avalon; loganne system map no longer includes lucos_comhra). UNRECOVERED monitoring alerts for those systems are stale window artefacts.
- 2026-06-03 Check 6 (/_info): `dns.l42.eu/_info` returns empty body but `lucos_dns` is healthy in monitoring — monitoring polls lucos_dns's /_info from a different endpoint than the public dns.l42.eu host. NOT a defect; don't re-chase. ⚠️ SUPERSEDED 2026-08-06 — I had this wrong for months: per `references/info-endpoint-spec.md`, Tier 1 is `system`/`checks`/`metrics` and **`checks`/`metrics` MAY be empty `{}`; only OMISSION is a defect**. The services I kept listing (configy, scenes, static_media, private, semweb, lukeblaney.co.uk, blog, tfluke, + several with `metrics: {}`) all PRESENT the keys, so they are spec-conformant and NOT defects — don't re-flag them. `ci` and `title` are Tier-2 (recommended) ⇒ missing either is never a defect. lucas42/lucos#212 exists but check its actual scope before citing it as covering "these".
- 2026-06-03 Check 5: deriving service domains for /_info checks — use configy `config/systems.yaml` (31 mappings); `installation/repositories` for the repo list (NOT orgs/lucas42 — 404).
- 2026-06-16: `lucos_root` container renamed to `lucos_root_app` on avalon (update Check 4 tracking key when next reviewed). Check 4 this run: docker_mirror trio + eolas_web + dns_sync all clean (eolas_web only benign nginx large-body buffering warns; dns_sync config-sync succeeds every 15min; registry only benign OnExpire noise).
- 2026-06-16: contacts circleci flap 06-15 15:33→16:06 = `Docker Login (mirror)` step 502+timeout because lucos_docker_mirror was redeploying (v1.0.37, containers restarted 15:32:01-34, deploySystem 15:33:09) while a Dependabot-merge build burst (#739) ran concurrently. build failed pre-deploy (prod untouched), self-recovered next pipeline. SAME root cause as lucos_deploy_orb#188 (fail-open mirror login) — commented there with the deploy-collision evidence, did NOT refile. If this recurs, it's #188.
- 2026-06-17 ops run: Monitoring 54/54 healthy. Loganne 36h: one weightings flap (drain-liveness 06-16 11:37→11:38, 1min). Root cause = FALSE POSITIVE: queue idle >120s then new event arrives, monitoring poll lands in the ~6s enqueue→process window, check sees "queue non-empty + last success >120s ago" → fires. Filed lucos_media_weightings#256 (P3, recommend failThreshold:2). 30d outages were all non-incidents: contacts 06-15 33min = known CI mirror-login collision (deploy_orb#188); mma reconcile_tag_names eolas 403 + weightings media-api-reachable 401 BOTH 06-14 ~10:31-10:33 = shared creds key-rotation auth-convergence transient (pattern_deploy_window_boundary_crossprobe_flap), recovered, green now; repos stale-dependabot-prs 06-14→15 = hygiene check working as designed (PR merged → recovered). Check 4: weightings/mail_smtp/arachne_triplestore/monitoring/router — all clean/benign (mail=internet SASL brute-force noise; router=scanner 404s; monitoring=failThreshold absorbing transients correctly). Monthly checks 5/6/7 not due (last 2026-06-03).
- 2026-06-24 ops run: Monitoring 54/54 healthy (0 failing/0 unknown). Loganne 7d: all flaps tracked or known-benign — arachne ingestor 06-17 12:15→12:27 single transient "Connection refused" (upstream blip, no recurrence, no file); repos stale-dependabot-prs re-alerts = hygiene check by-design; weightings fetch-info + mma metadata-api 06-18 ~07:08-07:27 1-min deploy-window cross-probe flaps; docker_health salvare-v4 06-19 2 short flaps = home-IPv6 transit. Check 3 30d "outages" both scheduled-job staleness (media_import all_files=#173 open/recovered 06-23; repos stale-dependabot-prs hygiene) — no incident reports needed. Check 4: firewall (clean, enforce stable), docker_health_app (2 isolated status=error age=1s heartbeat blips, benign), media_metadata_manager (clean), notes (single 25s burst of WS auth-401 06-24 07:20:47→07:21:12 = one expired-session client retrying every 5s then giving up — expected rejection; stack-trace-per-401 is a noise nit not worth filing at 8/10d), arachne_ingestor (healthy; benign /people/2 404 + reaping warning). Monthly 5/6/7 not due (last 2026-06-03, 21d). lucos_root→lucos_root_app rename applied to tracking.
- 2026-06-26 ops run: Monitoring 54/54 healthy (0 failing/0 unknown). Loganne 72h: 0 monitoringAlert/Recovery events — quiet. Check 3 30d: only candidate = lucos_repos stale-dependabot-prs hygiene check (alert→recovery 06-23 08:41Z, debug confirmed `stale-dependabot-prs` 2 unmerged PRs >48h, oldest contacts#741); by-design, recovered, no incident report. Check 4 (5 oldest): arachne_mcp/backups/mail_docs/photos_redis all clean (200s, normal redis RDB saves). lucos_root_app = scattered single-shot `502`/`context deadline exceeded` WARNs sweeping others' /_info (backups, eolas, creds, arachne, photos over 2d); root's sweep logs every transient w/ no retry/threshold; monitoring fully green = single-poll blips during brief restart windows, benign/not actionable. Monthly 5/6/7 not due (last 2026-06-03, 23d).
- 2026-06-29 ops run: Monitoring 54/54 healthy (0 failing/0 unknown). Loganne 48h: 3 flaps. (1) monitoring fetch-info 06-29 07:32→07:33 1min "HTTP Request timed out" = known self-probe flap (accept, #186 not_planned). (2) loganne webhook-error-rate 06-28 20:09→20:11 2min = brief genuine webhook-delivery transient to a dependsOn consumer, self-healed; already has failThreshold:2 + dependsOn deploy-suppression; logs rotated by 07:16 redeploy; no action. (3) backups create-backups 06-28 03:42→06-29 03:44 (~24h across runs) = transient codeload.github.com 400 Bad Request on VALID refs (puppet-conf/master, .github/main, lucos_media_player/main all verified valid), different repo each run, wget --tries doesn't retry 4xx → single transient repo miss reds the WHOLE run. Data safe (each repo re-archived next run, 14-24 instances). FILED lucos_backups#358 (P3): retry transient codeload HTTP errors + isolate per-repo failure from run status. Same amplification class as #298 (empty-repo, closed). Check 3 30d: only >30min candidate = that same backups flap (scheduled-job staleness, not a real outage) → no incident report. Check 4 (5 oldest): media_metadata_api/configy/locations_otrecorder clean; locations_otfrontend = benign per-request BrokenPipe tracebacks (local client disconnect) + PHP-scanner 401/404 noise; loganne = repeated "Event missing level, defaulting to routine" from deploy_orb events lacking `level` — server GRACEFULLY DEFAULTS, not a crash (distinct from client-lib pattern_loganne_client_level_required_arg), no action. Monthly 5/6/7 not due (last 2026-06-03, 26d; due ~07-03).
- 2026-06-27 ops run: Monitoring 54/54 healthy (0 failing/0 unknown). Loganne 36h: 0 monitoringAlert/Recovery events — quiet. Check 3 30d: only candidate = lucos_repos stale-dependabot-prs hygiene check (06-22 07:31Z→06-23 08:41Z; by-design, recovered) + media_import all_files recovered 06-23 (=#173) — no incident reports needed. Check 4 (5 oldest): semweb/lukeblaney_blog/lucos_authentication/lucos_locations_mosquitto/lucos_photos_api all clean or benign. semweb=PHP-scanner 404 probes + content-negotiation misses; blog=0 errors; authentication=1 url.parse DEP0169 warn + normal contacts polling (superseded by aithne); locations_mosquitto=external port-scanner TLS noise on public 8883 (unsupported protocol/no shared cipher/eof), healthcheck connects fine every 10s; photos_api=0 errors. Monthly 5/6/7 not due (last 2026-06-03, 24d; due ~07-03).
- 2026-06-30 ops run: Monitoring 53/53 healthy (0 failing/0 unknown). **System count 54→53 = lucos_authentication DECOMMISSIONED** (no longer running on avalon; replaced by lucos_aithne). Loganne 48h: 3 flaps. (1) backups backup-without-original 06-29 23:07→06-30 01:18 (~2h11m) = the authentication decommission tripping the OLD check logic; **RESOLVED by lucos_backups#360** (the #359 fix, merged 01:15:35Z, recovered 01:18) — check now only flags configy-declared volumes, decommissions no longer red it. (2) monitoring fetch-info 06-29 07:32→07:33 = known self-probe flap (accept, #186 not_planned). (3) loganne webhook-error-rate 06-28 20:09→20:11 = known brief webhook transient (has failThreshold:2+dependsOn). (2)&(3) already triaged in 06-29 run. Check 3 30d: both >30min candidates were lucos_backups scheduled-check staleness (06-29 create-backups #358 codeload transient; 06-30 backup-without-original decommission flap) — no incident reports needed. Check 4 (5 oldest): lukeblaney_co_uk (vuln-scanner 404 noise only), dns_bind (notify-refused to home secondary IPv6 at 06-29 22:48 restart = known #103/#104 home-IPv6 issue, primary green), photos_postgres (benign nightly-backup transaction WARNINGs), scenes/time (12 lines, clean). Monthly 5/6/7 not due (last 2026-06-03, 27d; due ~07-03).
- 2026-07-01 ops run: Monitoring 53/53 healthy (0 failing/0 unknown). Loganne 54h: 3 untriaged flaps, ALL explained. (1)+(2) aithne fetch-info/circleci 06-30 16:01 + fetch-info 16:35→16:38 AND docker_health `avalon` 15:56→16:14 (~18min, unhealthy container=lucos_aithne) = SAME cause: an aithne DEPLOY STORM (16 deploys 06-30 14:38→18:35Z, v1.20.32→v2.1.2; the v2.0.x major = KEK migration work). Pure deploy churn on the actively-developed service; docker_health can't deploy-suppress per-container so an 18min unhealthy window during a migration deploy is inherent/expected — no fix. (3) lucos_time `media` 07-01 07:20 1min flap = HEAD probe to staticmedia.l42.eu/time/big_00-00.mp4, already failThreshold:5, now ok — single transient, benign. backups + monitoring-fetch-info flaps in window = known (triaged 06-30 run). Check 3 30d: only >30min candidates = 2× lucos_backups scheduled-check staleness (06-29 #358 codeload, 06-30 #360 decommission) — no reports. The 06-30 aithne KEK ~46min outage IS covered (didn't surface in 30d script = alerts within deploy-suppression windows; report `docs/incidents/2026-06-30-aithne-kek-migration-deploy-race.md` merged via lucos#259). Check 4 (5 oldest): aithne/tfluke/photos_worker/arachne_explore CLEAN over 20d; media_seinn = single benign media-manager probe timeout (813ms→ceol.l42.eu) in 20d. Monthly 5/6/7 not due (last 2026-06-03, 28d; due ~07-03).
- 2026-07-02 ops run: Monitoring 53/53 healthy (0 failing/0 unknown). Loganne 54h: only 3 flaps, ALL already triaged in 07-01 run (aithne deploy storm 06-30, time `media` 07-01 07:20) — ZERO alert/recovery events in the ~24h since. Check 3 30d: only >30min candidates = 2× lucos_backups scheduled-check staleness (#358 codeload / #360 decommission) — no incident reports. Check 4 (5 oldest, all 06-14): TWO REAL FINDINGS. (1) **lucos_eolas_app**: `lucos_arachne` (UA) sending a MALFORMED non-JWT bearer token → eolas `JWT rejected: malformed token — Not enough segments` → treats as unauthenticated → `/metadata/all/data/` bulk ingest = **403**. Distinct from JWKS serve-stale gap (that's signature-verify fail, this is a PARSE fail = arachne putting a non-JWT string in Authorization). Onset 06-30 12:35, ongoing to 07-02 07:23 on current build 1.2.34 (redeploy didn't fix). arachne logs NOTHING re token acquisition (diagnostic gap). Filed **lucos_arachne#711** (P2, developer). (2) **lucos_repos_app**: C4 sweep regenerates artifacts every ~6h but commit fails `403 Resource not accessible by integration` (App lacks Contents:write) → docs/c4/ frozen at 06-14 snapshot → **this is the root cause of repos#444** (stale C4 shows decommed authentication). Commented root cause on #444; fix = grant Contents:write to lucos_repos App (sysadmin/lucas42). arachne_web/contacts_app/contacts_db CLEAN. Monthly 5/6/7 not due (last 2026-06-03, 29d; due ~07-03).
- 2026-07-05 ops run: Monitoring 53/53 healthy AFTER remediation (was 52/53: lucos_backups failing on `prune-backups`). **ROOT CAUSE (verified from traceback, not label): NOT a network issue** — the debug said `Prune failed for: xwing.s.l42.eu (host unreachable)` but xwing was reachable (IPv6 2/2 7ms from avalon) and the remote `find` actually RAN and produced valid stdout. `find` exited 1 only because 2 rotated github-archive files (`lukeblaney_cv_tailored`/`lucos_file_sync` .2026-06-28.tar.gz) vanished mid-traversal (TOCTOU race with concurrent archiving/replication) → stderr `No such file or directory` → exit 1. `shell.py:find_backup_files` uses `connection.run` (raises on non-zero) → `prune-backups.py` mislabels ANY error as "host unreachable" AND fails the whole run. Same amplification class as #358 (create-backups codeload). **Filed lucos_backups#365 (suggest P3).** REMEDIATION: triggered ad-hoc `docker exec lucos_backups pipenv run python -m scripts.prune-backups` at 10:16Z (no concurrent archiving) → "Pruning Complete - 0 pruned" all 4 hosts → check GREEN (verified monitoring 53/53). NB `${PIPESTATUS}` fails in container's POSIX sh (bad substitution) — harmless. Prune cron = `49 4 * * *`. Note 07-04 BOTH salvare+xwing failed prune (label-only, salvare recovered 07-05 — cause not individually confirmed, likely same race or the salvare IPv6 flap #185-closed). Check 2 Loganne: (a) circleci estate-storm 07-02 15:16→15:56 ~40 systems simultaneous = CircleCI-API/UnknownsGate (recovered, non-incident, pattern_circleci_unknownsgate_estate_storm); the Check-3 script shows these as "UNRECOVERED …:_circleci" = PARSING ARTIFACT (alert hr "…: circleci" vs recovery "All checks healthy on X" don't pair) — NOT real. (b) arachne `search` 07-02 21:53→21:54 flap: check already `failThreshold=3` so it crossed a genuine ~3-min brief Typesense-probe unavailability, self-recovered, NO error in search-container logs, ingestor idle then, no recurrence 72h — noted, watch for recurrence, no file (impact-vs-effort; check already well-thresholded). Check 3: no incident reports needed. Check 4 (5 oldest, all 06-15): contacts_web/creds_configy_sync/creds_ui/eolas_db CLEAN; contacts_googlesync_import = single benign transient 502 to contacts.l42.eu/people/import 07-03 09:55 (5-min job self-healed). Check 5: 0 non-healthy circleci across 49 systems — no repo red. Check 6 (/_info sweep 32 domains): 30 OK all top-level fields; dns.l42.eu+dns2.l42.eu EMPTY = KNOWN-EXPECTED (public host empty, monitoring polls internally, both healthy — not a defect); empty checks/metrics on configy/scenes/static_media/private/semweb/lukeblaney.co.uk = lucos#212 P3. Check 7: LE 200/DockerHub 401/CircleCI 401/GitHub 200 all nominal. Monthly 5/6/7 now run (were overdue, last 2026-06-03).
- 2026-07-09 ops run: Monitoring 53/53 healthy (0 failing/0 unknown; worlds not yet a monitored system, so still 53). Container count now **56 on avalon** (lucos_worlds_web + lucos_worlds_db added 07-07). Loganne since 07-05: 3 flaps, ALL new-service bootstrap churn from **lucos_worlds** stand-up (BookStack worldbuilding, deployed 07-07, login+RBAC complete 07-08 per project memory). (1) backups `volume-host` 07-07 18:07→23:07 (~5h): "Volumes not found on host: lucos_worlds_db_data, lucos_worlds_web_storage" — volumes declared in configy before the worlds containers first started/created them; recovered 23:07 when containers came up. SAME pattern as aithne credential_store bootstrap flap (2026-06-11 note). (2) docker_health `avalon` 07-07 22:45→22:52 (7min): "Unhealthy containers: lucos_worlds_web" during initial deploy, self-recovered. (3) monitoring fetch-info 07-07 08:02 (1s, post-selfRestart) = known self-probe flap (#186). No files/reports — textbook bootstrap flaps, self-resolved, analogous to documented aithne pattern. Check 3 30d: only non-artifact >30min entries = backups 07-05 (#365 prune, handled last run) + backups 07-07 (this worlds volume-host flap) — both scheduled-check staleness, no user impact, no incident reports. The `:_circleci` UNRECOVERED entries = known 07-02 estate-storm parsing artifact (pattern_circleci_unknownsgate_estate_storm). Check 4 (2 oldest distinct + docker_mirror + 2 new worlds): dns_sync/eolas_web/docker_mirror_registry all clean-or-benign-known (eolas nginx large-body buffering; registry OnExpire "operation unsupported" noise); worlds_web CLEAN (custom-init RBAC scripts exited 0, admin external_auth_id=worlds:admin set); worlds_db all-benign (io_uring EPERM→libaio fallback from host `kernel.io_uring_disabled=2` hardening sysctl; unauthenticated aborted-conn = linuxserver TCP DB-readiness probe pattern_mariadb_unauth_abort_is_nc_dbwait_probe). Monthly 5/6/7 NOT due (last 2026-07-05, 4d).
- 2026-07-13 ops run: Monitoring **55/55 healthy** (0 failing/0 unknown; count 53→55 = lucos_worlds now monitored + likely 1 more since 07-09). Loganne since 07-09: 2 flaps. (1) monitoring fetch-info 07-09 12:12→12:14 (2min, right after 11:44 selfRestart) = known self-probe flap (#186, accept). (2) **lucos_repos `audit` 07-10 11:35→14:24 (~2h49m)** = NEW. Confirmed from persisted alert debug: "sweep incomplete: 54 convention check(s) skipped due to API errors" easing 54→10→0 = GitHub secondary rate-limit window during a deploy-heavy morning (~12 deploys 07:00-13:10Z). Container restarted TODAY 07-13 07:38 so 07-10 logs gone (log-buffer artifact) — used Loganne failingChecks[].debug instead. Code already has TWO resilience layers (conventions/ratelimit_transport.go 5-min max-wait + audit.go retry-tail pass) that were outlasted; incomplete-sweep-aborts is deliberate+tested (findings not deleted on partial data). Internal-only, self-recovering, no user impact. **Filed lucos_repos#465 (P3)** with explicit impact-vs-effort framing (likely legitimate WONTFIX/accept-risk; value = saves re-investigation). Check 3 30d: two >30min outages = repos audit (this) + backups 07-07 worlds-volume bootstrap (already triaged 07-09) — both internal self-recovering, NO incident reports. Check 4 (5 oldest): docker_mirror_web (0 err lines) / docker_mirror_info (6 lines, benign) / arachne_triplestore (20k Fuseki query lines, 0 errors) CLEAN; media_weightings = 16 `WARN SLOW: GET /_info 200` 1-2.6s ALL post-restart cold-cache warmup (restarted today 07:11, tapering), healthy+no flap, benign; mail_smtp = 636k lines ALL external SASL brute-force noise (known benign, host/firewall not app). Monthly 5/6/7 NOT due (last 2026-07-05, 8d).
- 2026-06-11: lucos_aithne is a NEW service (replacing lucos_authentication, tracking lucos_aithne#12) under heavy active development (v1.1.x→v1.12.0, 30+ deploys 06-09→06-11). Its 06-09→06-10 flaps (fetch-info/tls/db) = deploy churn + new-service cert latency (lucos_router#95). Its new volume `lucos_aithne_credential_store` appearing on avalon before being declared in configy caused the backups volume-config/volume-host flaps 06-09 23:08-23:53 (same root cause; now declared with recreate_effort: considerable, green). Don't re-investigate these as separate incidents while aithne is being stood up. Bootstrap warning (BOOTSTRAP_ADMIN_CONTACT_ID still set post-enrolment) is lucas42's WIP — he set it.
- 2026-07-14 ops run: Monitoring **54/55 healthy, 1 failing = lukeblaney_co_uk `circleci`**. Check 1 ROOT-CAUSED + FIXED: `check-links` red since 07-14 01:05 (blocking `deploy-avalon`; site itself green throughout — tls+fetch-info fine). Job fails exit **127** (command-not-found) at `linkchecker` DESPITE pip reporting "Successfully installed linkchecker-10.6.0". Repo unchanged (last pass job 448 on 06-16; the #67 Dependabot merge only touched GH-Actions pins). **CAUSE (verified from `Spin up environment` log, not inferred): `ubuntu-2204:current` is a rolling tag and MOVED BACKWARDS** — job 448 (pass) got image `ubuntu-2204:2026.05.1` py3.14 (cp314 wheels); jobs 461/466 (fail) got `ubuntu-2204:2025.09.1` py3.13 (cp313). On the older image the pip console script doesn't resolve on PATH. Re-run from failed (job 466) reproduced identically on same image ⇒ **deterministic, a re-run can NEVER clear it**. FIX = install into explicit venv + invoke `/tmp/linkchecker-venv/bin/linkchecker` by absolute path (packaging guarantees script location; image PATH layout becomes irrelevant). Deliberately did NOT pin the image — pinning forward targets an image the tag already moved away from. **VERIFIED: pipeline 185/job 473 check-links SUCCESS on the SAME `2025.09.1` image** (133 links, 0 errors) ⇒ fix works on the broken image, not CircleCI repairing the alias. **RESOLVED SAME RUN**: PR lukeblaney_co_uk#68 approved by code-reviewer + lucas42 (both on head a1670b4), auto-merged 09:31; main pipeline 186 FULL SUCCESS incl. `lucos/deploy-avalon`; Loganne `Deployed lukeblaney_co_uk v1.0.26 to avalon` 09:33:51Z; monitoring re-verified **55/55 healthy, 0 failing, 0 unknown**. (lukeblaney.co.uk `/_info` has empty checks/metrics = known lucos#212 P3, not a defect.) Estate exposure CHECKED + CONTAINED: only 2 repos use `ubuntu-2204:current` — lukeblaney_co_uk (fixed) and lucos_deploy_orb/.circleci/test-deploy.yml (docker-only, NO pip/PATH-dependent installs ⇒ not exposed, CI green). No estate issue filed.
- 2026-07-14 Check 2 (Loganne 36h, 9 events): (1) **circleci flaps blog 03:48→03:51 / repos 03:49→03:51 / tfluke 03:52→03:55** = a GENUINE ~26-min intermittent **CircleCI API degradation**, proven from monitoring container logs: **271 `CircleCI API request failed ... timeout` lines across 52 DISTINCT repos, 03:30–03:56**. Only 3 systems alerted because the UnknownsGate needs 5 CONSECUTIVE unknowns (~5min) — timeouts were ~every-5th-poll per repo, so most never tripped. **This VINDICATES the threshold-5 value** (lucos_monitoring#279, implemented+closed, value signed off by lucas42 2026-06-09). Alerts were CORRECT (those 3 genuinely had no CI signal 5+ min). NO FILE — filing another bump would revisit a signed-off value on evidence that supports it. Baseline: ~10-15 CircleCI timeouts/repo per 27d is NORMAL background; gate absorbs ~all. (2) **arachne `search` 07-13 17:08 (~1min)** = RECURRENCE of the 07-02 21:53 flap → **FILED lucos_arachne#735** (suggest Medium). (3) NB my own `rerun` of a failed workflow caused a spurious `monitoringRecovery` at 09:23 then a fresh alert — **the `circleci` check reads the MOST RECENT workflow, so an in-flight re-run reads as healthy**; don't mistake that for a real recovery.
- 2026-07-14 arachne#735 detail: `checkSearch()` (explore/src/server/index.js:53-66) probes Typesense with `AbortSignal.timeout(450)`; catch returns bare `err.message` ⇒ debug "The operation was aborted due to timeout" (exact observed string). failThreshold=3 ⇒ 3 consecutive 60s polls each >450ms. **My first hypothesis (tight timeout / false positive) was WRONG and measuring killed it**: 12 live probes from inside lucos_arachne_explore = `230, 4, 5, 5, 4, 6, 3, 3, 10, 4, 3, 2` ms ⇒ p50 ~4ms, so 450ms carries 45-100x headroom (the 230ms is cold-connect — eats half the budget). So the alert is almost certainly CORRECT: a REAL ~3-min Typesense degradation, 2x in 11 days. search container `restarts=0 oomkilled=false` ⇒ never crashed, slow-but-alive or network. **Real defect = the probe DISCARDS its own measurement**: no elapsed-ms in debug, NO search-latency metric (only `sparql-query-latency-ms` exists), nothing logged ⇒ container restart (07-14 07:19) erased the 07-13 evidence permanently. Tell: `sparql-latency` (a LATENCY check) gets 1100ms + WARN_MS=1000; `search` (a LIVENESS check) gets 450ms — inverted. Fix = mirror checkSparqlLatency (line ~116): elapsed-ms in debug + `search-latency-ms` metric. Do NOT raise the 450ms budget (would hide real signal).
- 2026-07-14 Check 3 (30d): 3 >30min outages, ZERO incident reports needed. backups 07-07 (worlds volume bootstrap, triaged 07-09), repos 07-10 (#465 rate-limit, triaged 07-13), lukeblaney_co_uk 07-14 8h16m (= the circleci check; **site never went down** — tls+fetch-info green throughout; CI/deploy blockage on a personal static site, no user-visible impact ⇒ PR#68 + commit message capture the learning; per Check-3 step-3 "skip for internal-only"). NB the 09:23 "recovery" in the script output was my own in-flight re-run, not a real recovery.
- 2026-07-14 Check 4 (6 containers; 0 overdue @60d, 0 @30d — oldest was 06-17/27d): **lucos_monitoring** (415 error-ish lines since 06-17 = ALL `CircleCI API request failed: timeout` warnings, the Check-2 background baseline; healthy, restarts=0); **lucos_router** (5.7M lines — ALL external scanner/bot noise on default server `server: _` with bogus Host headers nextcloud.cayrier.com/forum.casual.fr/raw-IPs probing /.env, /*.php, /robots.txt, dir-listings; known benign, correctly rejected); **lucos_schedule_tracker** (2 "Failed to send error page" + /error_log.php scanner probe — benign); **lucos_creds** (32 `ssh: no common algorithm for host key` WARNs = 8 each × 4 key types, external scanners probing :2202 offering non-ed25519 — benign); **lucos_media_manager** (5 `java.net.SocketException: Broken pipe` + 5 `ERROR: Unknown Error (Class:LongPollControllerV?)` = client-disconnect-mid-longpoll, same benign class as otfrontend BrokenPipe; 5/7d, noise nit not worth filing); **lucos_locations_oauth2_proxy** (NEW tracking key, added to avalon — container count now **57**). Monthly 5/6/7 NOT due (last 2026-07-05, 9d).
- 2026-07-15 ops run: Monitoring **55/55 healthy** (0 failing/0 unknown). Container count 57 on avalon. **Deploy/restart burst this morning ~07:20-07:31 (29 containers "hours ago")** ⇒ Check-4 lookback effectively truncated to ~6h for most containers (pattern_container_restart_log_buffer_artifact — a `--since 06-24` that returns few lines is NOT 3 weeks of clean logs). Check 2 (Loganne 40h, 5 events): 3× lukeblaney_co_uk 07-14 09:23/09:24/09:32 = already triaged in 07-14 run (my own in-flight rerun artifact + real recovery post-PR#68). 2× **lucos_locations `location-freshness` 07-15 19:01:59→20:49:22 (1h47m) = FIRST REAL FIRING OF #91, working exactly as designed.** Genuine ~31h42m CLIENT-side gap: phone `cheetah` last fix 07-14 13:01:25Z, cleanly `closed its connection` 13:05:35Z (clean close ⇒ NOT the duplicate-session flap of 06-29), no reconnect until 07-15 20:48:20Z, first fix 20:48:24Z → recovery 58s later. Server exonerated: mosquitto/otrecorder/otfrontend all StartedAt 07-10, restarts=0, zero errors. Alert fired at 108034s = 34s after crossing the 108000s (30h) threshold. Caught in 30h what previously went unnoticed 3.5d (06-29) / weeks (#5). **NO incident report** — our service never failed, gap originates outside our control boundary, nothing server-side could prevent/recover it, and the recurrence-prevention (#91) is already shipped and is what caught it. Check 3 (30d): 3 outages >30min — locations 07-15 (this), repos 07-10 (#465, triaged 07-13), lukeblaney_co_uk 07-14 (CI-only, site never down, triaged 07-14) ⇒ **0 incident reports needed**. Check 4 (5 oldest): arachne_search (restarted 07-15 07:30 — AGAIN erasing #735 evidence, 2nd day running; only benign Typesense raft `Peer refresh failed: Doing another configuration change` 9s post-start), mma_exporter (124 lines, 0 errors), arachne_ingestor (1 BrokenPipeError on a `/_info` poll from monitoring 192.168.208.5 = benign client-disconnect), media_metadata_manager (1 transient `metadata-api probe failed ... latency: 1231ms` 13:31:32, no alert fired, check absorbed it, benign), notes (6 lines clean). Monthly 5/6/7 NOT due (last 2026-07-05, 10d).
- 2026-07-15 **⚠️ MY OWN PROBE METHODOLOGY BIT ME TWICE on lucos_media_metadata_manager** — nearly fabricated a media-api outage from a broken probe. (1) `docker exec ... wget https://…` → 5/5 "FAIL" in 1-4ms: **`wget: not found`** in that image (also no `time` builtin — it's a minimal PHP/Apache sh). A sub-5ms "failure" is too fast to be a real network call — treat implausibly-fast failures as a broken probe, not an outage. (2) `docker exec ... php -r 'file_get_contents(...)'` → FAIL 50ms; surfacing the suppressed warning (drop the `@`) gave **`HTTP/1.1 401 Unauthorized`** — the CLI probe doesn't carry the auth header the app's real probe sets via stream context. **Rule: reproduce the app's EXACT authenticated call, and always surface the error string before concluding.** Monitoring said 55/55 + all 8 media systems healthy throughout — trust that over a hand-rolled probe.
- 2026-07-14 **lucos_locations oauth2_proxy crash-loop RESOLVED** (was pattern_locations_oauth2proxy_sidecar_crashloop, 07-10 ~51min incident): container `restarts=0 oom=false`, up since 07-10 00:33, ZERO error lines in 7d. Live-probed the human path (which /_info CANNOT see by design): `/map/`→**302** (auto-redirect, i.e. **#99/#100 "split" SHIPPED** — memory said "CHANGING", now confirmed live), `/oauth2/start`→302 to aithne with correct `client_id=lucos_locations` + `scope=openid email profile locations:read` + redirect_uri, device `/owntracks/pub`→**401** (basic-auth intact), `/_info`→200. Full stack healthy. NB oauth2_proxy has `health=none` (no healthcheck) — the known /_info blind spot persists structurally, but no evidence of impact.
- 2026-07-18 ops run: Monitoring **55/55 healthy** (0 failing/0 unknown). Check 2 Loganne (4d, 307 events): **153 alerts, 152 of them `circleci` = ONE estate-wide CircleCI API outage 07-16 22:35→07-17 00:07 (~1h30m), ~50 systems.** Verified upstream-only, NOT ours: monitoring container logged **1369 `CircleCI API request failed: timeout` lines** in the window, debug strings then became `502 Bad Gateway from pipeline endpoint`; token fine (api/v2/me→200 now); and **zero** outbound failures in the same window across repos_app/root_app/dns_sync/arachne_ingestor/contacts_googlesync_import ⇒ no shared avalon network fault (coincidence hypothesis killed with evidence, not assumed). NO FILE: `fetcher_circleci.erl` maps non-200/404 → `make_third_party_probe_check` → `ok: unknown` → UnknownsGate; alerts only fired after 5 consecutive unknowns, i.e. the gate worked and we genuinely had no CI signal for 90min. Same class as 07-02 storm (pattern_circleci_unknownsgate_estate_storm). lucos_monitoring has **zero** open issues (verified with a positive control after an empty search — arachne#735 returned fine). Check 3 (30d): every >30min entry is that one storm, plus locations 07-15 (triaged 07-15) + lukeblaney_co_uk 07-14 (triaged 07-14) ⇒ **0 incident reports needed**. The `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact (55/55 green proves it).
- 2026-07-18 Check 4 (5 oldest: docker_health_app, firewall, backups, mail_docs, photos_redis — 0 overdue @60d/@30d, oldest 24d): firewall/mail_docs/photos_redis **0 error lines**; docker_health_app 7 lines = known-benign heartbeat blips + 2 `connection refused` to schedule-tracker during its restarts. **lucos_backups = REAL FINDING → filed lucas42/lucos_backups#374 (suggest P3).** Every hour at **:07** the single-threaded `HTTPServer` (src/server.py:310 — NOT ThreadingHTTPServer) is blocked ~20s by the `7 * * * *` cron POST to `/refresh-tracking` (src/backups.cron:3), which enumerates GitHub repos over the network. Evidence: root_app logged **96 `context deadline exceeded` on backups.l42.eu in 7d, 100% at minute :07** (backups = 192 of root's timeout lines estate-wide; next-worst host = 2 each); backups' own **257 BrokenPipeError traceback lines, also 100% at :07** — SAME root cause (queued clients give up, server writes to closed socket), not a separate nit; access log 07-18 shows a 21s gap (09:06:53→09:07:14) in a 10s healthcheck cadence with the backlog draining the instant the POST returns. Escalation path: block length is bounded by **GitHub's** responsiveness — on 07-16 23:07 (during the CircleCI/GitHub degradation) refresh-tracking took a `503` from api.github.com and POST returned 500. Monitoring has NOT alerted on backups fetch-info in 30d (60s poll rarely lands in the window) so impact today = hourly root-dashboard fetch failure + ~46 tracebacks/day; fix is one line. Caveat noted in issue: check BackupsHandler for shared mutable state before threading.
- 2026-07-18: Monthly 5/6/7 NOT due (last 2026-07-05, 13d).
- 2026-07-19 ops run: Monitoring **55/55 healthy** (0 failing/0 unknown). Container count **57** on avalon. Check 2 Loganne (page covers 07-12→07-19, 400 events): **ZERO monitoringAlert/Recovery events since 2026-07-17T00:07** — i.e. genuinely quiet for the ~2 days since the last run. All 159 alerts in the page are the 07-16 22:35→07-17 00:07 CircleCI-API storm (triaged 07-18, upstream-only) bar 7, and all 7 are already-triaged (arachne#735 07-13, CircleCI degradation + lukeblaney_co_uk#68 07-14, locations #91 first-firing 07-15). Check 3 (30d): every >30min entry is that one storm plus locations 07-15 + lukeblaney_co_uk 07-14 ⇒ **0 incident reports needed**; `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact (55/55 green proves it). Check 4 (5 oldest: root_app, arachne_mcp, locations_mosquitto, photos_api, semweb — 22-23d, **0 overdue** @60d/@30d): arachne_mcp (17.7k lines, **0** error lines) and photos_api (23.8k lines, **0** error lines) CLEAN. semweb 770 `[php:error]` = 100% scanner probes for nonexistent files (wp-login.php, shell.php, xmrlpc.php, admin.php) via router 192.168.128.1 — benign, same as 06-27 assessment. mosquitto 17,060 "error" lines = external TLS scanners on public 8883 (9466 unexpected-eof + 3737 protocol-error + unsupported-protocol/version-too-low/no-shared-cipher) — known benign. **root_app: 118 of 133 error lines are `context deadline exceeded` on backups.l42.eu, 118/118 at minute :07** — textbook confirmation of lucas42/lucos_backups#374 (already Ready/Low/owner=lucos-developer, design settled in comments); rate steady ~16-21/day, NOT worsening; deliberately did NOT comment (issue already fully specified — a "still happening" note would be noise). Monthly 5/6/7 NOT due (last 2026-07-05, 14d).
- 2026-07-23 ops run: Monitoring **55/55 healthy NOW** (0 failing/0 unknown), but Loganne was NOT quiet. **DOMINANT SIGNAL: lucos_creds_configy_sync unhealthy ~55min/hour for ~2.5 DAYS (07-19 10:17Z → 07-21 22:55Z, ~55 alert/recovery cycles).** Manifested as BOTH `lucos_docker_health:avalon` hourly flap AND `lucos_creds:configy_sync` alerts — SAME root cause (confirmed via docker_health_app logs: "Reporting unhealthy: Unhealthy containers: lucos_creds_configy_sync" every minute 22:00-22:54). **Cause (timing-correlated, mechanism from code — NOT fully proven from flap-window logs, which are gone: container restarted twice since, StartedAt now 07-23 07:18):** CONFIGY_SYNC_PRIVATE_SSH_KEY rotated 07-19 10:04Z (prod) → onset 10:17Z; re-stored correctly 07-21 22:47/22:48 + lucos_creds v1.3.81 deploy 22:54Z → recovery 22:55Z. Two intermediate key updates (07-19 23:57, 07-20 00:29) did NOT fix. startup.sh runs `set -e` and its foreground/keepalive process is the final `cat <> /var/log/cron.log`; the SSH-key validation block (`exit 1` on CR/`~`/missing-PEM, the creds#474 [[pattern_multiline_secret_truncated_at_first_line]] guards) sits BEFORE that line → an invalid-format key crash-loops the container under `restart: always` → docker unhealthy. **DECISION: no new issue, no incident report.** (a) RESOLVED — 48h quiet, healthy, syncing hourly (verified: "Sync Complete" every :53 through 21:53 today, end-to-end not just healthcheck). (b) Operator-driven credential rotation; lucas42's own repeated key re-stores over the window culminating in the fix — he knew it was down. (c) Root-cause THEME already tracked: creds **#473** (malformed values + missing write-time validation), **#482** (post-save readback to catch shape-valid-but-wrong values), **#483** (regression in #476 validation). (d) NO monitoring gap: sync.py imports schedule_tracker + reports failures; crash-loop path caught by docker_health, valid-but-wrong-key SSH-auth-fail path (BatchMode yes, ssh timeout=1) caught by schedule_tracker. (e) NO downstream impact — 55/55 green throughout except the two derived checks. Healthcheck `test -p /var/log/cron.log` only checks the FIFO exists, but the two failure paths are covered elsewhere, so not a silent-failure risk → no ticket. Flagged to team-lead as informational. **Minor Loganne items:** eolas `places-in-universe` 07-21 13:39→13:41 + 13:42→13:44 (2 brief flaps, ~5min, failThreshold=1, graph-integrity check "all non-fictional places reachable from Universe" — transient mid-edit inconsistency, self-corrected, ok=True now, logs gone; WATCH for recurrence, no file — raising failThreshold would delay real integrity-break detection); locations `location-freshness` 07-20 23:02→23:03 (1min, known #91 cheetah client-side gap); creds `circleci` morning flaps 07-19/20/21 ~07:xx (known CircleCI-API morning degradation, pattern_circleci_unknownsgate_estate_storm). Check 3 (30d): every >30min outage is either the 07-16 22:35→00:07 CircleCI-API estate storm (triaged 07-18, upstream-only; the real-system-name entries like media_seinn/arachne/eolas 07-16 ~22:51-23:56 are the `circleci` check on those systems — script extracts system name regardless of check) OR the configy_sync incident (above) ⇒ **0 incident reports needed**. `:_circleci` UNRECOVERED lines = known humanReadable-parsing artifact (55/55 green proves it). Check 4 (5 oldest: lukeblaney_blog/media_metadata_api/loganne/configy/locations_otfrontend): all CLEAN or benign-known — blog=6 scanner 404s; media_metadata_api=**grep false-positive** (53 "err-ish" = normal lucos_media_import PUT track updates whose base64 filterValue fingerprints contain fail/error substrings, 0 real errors — remember to exclude INFO lines); loganne/configy=0 errors (restarted today/40h ago, buffer artifacts); otfrontend=989 nginx WordPress-scanner 404 probes (benign). Monthly 5/6/7 NOT due (last 2026-07-05, 18d; due ~08-05).
- 2026-07-19 Check 4 note — **locations `cheetah` client-side gap recurred sub-threshold**: mosquitto shows the phone disconnecting 07-18T12:39:27 and not reconnecting until 07-19T06:41:17 = **18h02m gap**, below the 30h `location-freshness` threshold (#91), so correctly NO alert. Confirms the 07-15 pattern is recurrent client-side behaviour, not a one-off, and that the 30h threshold is doing its job (didn't fire on a benign overnight gap). Server side exonerated again: mosquitto/otrecorder/otfrontend restarts=0. No action — outside our control boundary.
- 2026-07-27 ops run: Monitoring **55/55 healthy** (0 failing/0 unknown). Container count **57** on avalon. **⚠️ METHODOLOGY FIX (the main output of this run): Loganne `/events` ignores `limit` entirely** — `limit=50`, `limit=2000` and `limit=3000` ALL return the same fixed ~470-event (~7-day) page, as do `offset`/`page`/`before`/`from`/`start`/`days`/`system`. **`since=YYYY-MM-DD` is the ONLY honoured param** (`type=` also works). So the Check-3 command as documented had been silently covering ~7 days while reporting a "30-day" result — for weeks. Fixed `agents/sre-ops-checks.md` (Checks 2+3 now use `since=`, plus a mandatory print of the fetched min/max date range). NB `since` is **event-bounded not date-bounded**: caps at ~2278 events (`since=2026-05-01` still only reaches 2026-06-30) ⇒ ~27d, so always scope claims to the printed range. Check 2 (Loganne, true 7d): only ONE new alert since the 07-23 run — **lucos_locations `location-freshness` 07-26 20:33:12→22:57:40 (2h24m)**, everything else pre-dates 07-23 and was already triaged. Root cause CONFIRMED client-side from mosquitto + alert debug (not inferred): debug `108052s` at onset and `116659s` at recovery both back-compute to last data **2026-07-25 ~14:32Z** ⇒ real gap 32h25m, alert fired 52s after crossing the 30h (108000s) #91 threshold. mosquitto shows `cheetah` "exceeded timeout, disconnecting" 07-25 20:37:45, a zombie reconnect 07-26 00:47:25→02:17:26 that published nothing (k3600 keepalive ⇒ half-dead conns linger ~1.5h), then real reconnect **22:57:04** → recovery 36s later at 22:57:40. Server exonerated: mosquitto/otrecorder/otfrontend all StartedAt 07-10, restarts=0, oom=false. **4th occurrence of the same client-side pattern** (07-15 31h42m fired, 07-18→19 18h sub-threshold, 07-20 1min, now 07-26 32h25m) ⇒ recurrent phone behaviour outside our control boundary, #91 doing exactly its job. NO FILE, NO REPORT (consistent with 07-15 disposition). Check 3 (**true 30d for the first time in weeks**): every >30min entry accounted for — the 07-16 22:39→07-17 00:07 CircleCI-API estate storm (~50 systems, triaged 07-18, upstream-only); creds 07-19→07-21 62h + ~55 hourly docker_health cycles = configy_sync key corruption, **incident report EXISTS: `docs/incidents/2026-07-19-configy-sync-key-corruption.md`** (⚠️ this SUPERSEDES my 07-23 note saying "no incident report" — a report was written afterwards; refreshing `~/sandboxes/lucos` is what caught it); backups 07-05 (#365) + 07-07 (worlds bootstrap); repos 07-10 (#465); locations 07-15 (#91) + 07-26 (this run); lukeblaney_co_uk 07-14 (CI-only, site never down); aithne 06-30 (report exists). ⇒ **0 incident reports needed.** `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact (55/55 green proves it); note many are CI-only repos with no deployed service (lucos_navbar, restful-queue, vue-leaflet-antimeridian, lucos_deploy_orb, .github). Monthly 5/6/7 NOT due (last 2026-07-05, 22d; due ~08-05).
- 2026-07-27 Check 4 (6 oldest, 06-29/06-30 band, 28d — **0 overdue @60d, 0 @30d**): otrecorder (196k lines, **0** err-ish), scenes (83 lines, 0), lucos_time (0 errors, but **restarted TODAY 07:08 ⇒ only ~6h buffer**, log-buffer artifact), dns_bind (AXFR-transfer-denied probes = correctly refused + the known home-secondary IPv6 `notify … connection refused` at the 07-21 restart, #103/#104) and lukeblaney_co_uk (155k lines = 100% scanner 404 probes for error-logs/.env + Apache `AH01630 denied` on .htpasswd/.htaccess, i.e. the deny rules WORKING) all benign. **lucos_photos_postgres — nearly filed a false finding, worth remembering:** 25 `ERROR: duplicate key value violates unique constraint "person_contact_id_key"` on `UPDATE person SET contact_id=…` (24 in a ~20min burst 07-13 10:13-10:35, 1 on 07-19; NOT ongoing). These are **the app's designed 409 path, not a defect** — `api/app/routers/people.py:254-266` catches `IntegrityError`, rolls back, looks up the conflicting person and raises `HTTPException(409, {"message": "A person with this contactId already exists", "existingPersonId": …})`. A unique constraint rejecting a genuinely-invalid link (same contact → 2 people; contact_id 70 attempted 3×, 65 twice) is correct behaviour. Postgres logs every handled constraint violation at ERROR level ⇒ **a DB ERROR line is not evidence of an application failure; check whether the app catches it before counting it.** Also 1× `relation "photo" does not exist` (07-19 09:15, raw SQL `SELECT … FROM face f JOIN photo m …`) = **ad-hoc psql query with a wrong table name, NOT code** — verified absent from `origin/main` (the app is SQLAlchemy ORM throughout, no raw SQL of that shape). 44 benign `there is already/no transaction in progress` WARNINGs across 8 days unchanged.
- 2026-07-31 ops run: Monitoring **55/55 systems and 213/213 individual checks healthy** (0 failing/0 unknown) — verified live at the end of the run. Container count **57** on avalon. **Context: estate-wide deploy burst 07:09–07:47Z** rolling out `lucos_loganne_pythonclient 2.0.5` + `lucos_schedule_tracker_pythonclient 2.0.7` (both **major** bumps) to ~30 systems. Two consequences: (a) transient `502` blips on arachne/loganne/ceol/l42.eu `/_info` polls, all inside restart windows and all self-cleared (monitoring showed `pending_verification`/`unknown` mid-run, then 0 unknown); (b) **Check-4 log buffers wiped estate-wide** — 40 of 57 containers had StartedAt within the hour ([[pattern_container_restart_log_buffer_artifact]]), so the four 30-day-old containers (photos_worker, arachne_explore, media_seinn, tfluke) were deliberately NOT reviewed rather than reviewed against a 20-minute buffer; they stay at 07-01 and take priority next run. **Post-rollout sweep of ALL 57 containers for errors since 07:00 = only 3 hits, all known-benign**: media_manager `LongPollControllerV3` BrokenPipe (client disconnect mid-longpoll), tfluke `Fetching National Rail data not yet implemented` (Scrapy bot hitting an unimplemented route, matches router 500), mosquitto external TLS scanner noise on 8883. **Zero breakage from the 2.0.x client majors** — worth knowing given [[pattern_loganne_client_level_required_arg]].
- 2026-07-31 Check 2 (Loganne, fetched 1992 events 07-05→07-31 = 26d): 19 alert/recovery events since the 07-27 run. **TWO NEW FILES.** (1) **lucos_media_seinn `media-manager` 07-30 05:24:30→05:25:10 + 05:26:37→05:27:11** (two 40s/34s flaps, `failThreshold: 2` ⇒ ≥2 consecutive timed-out polls each). **media_manager was demonstrably fine**: router access log for ceol.l42.eu 05:20–05:30 has ZERO non-2xx AND seinn's own `lucos_media_seinn`-UA `GET /v3/poll` returned 200 every ~10s straight through both windows. ⚠️ Caveat I stated in the issue: router log format has no `$request_time`, so this proves media_manager *responded*, not that it responded *fast* — slow-but-successful is not excluded. Live-measured the exact authenticated call from inside the container: 20 probes = 118ms cold then p50 ~10ms (7–29ms) vs an `AbortSignal.timeout(800)` budget ⇒ ~80x headroom, so "tight timeout" is a weak explanation (same conclusion as arachne#735, reached the same way). **Defect: v3.js computes `probeMs` (line 67) and logs it via console.warn (line 68) but line 70 puts only `error.message` in `debug`** ⇒ the number lands in a volatile container log that a daily redeploy erases, while the durable Loganne alert carries nothing. Filed **lucos_media_seinn#583** (suggest P3, one-line fix). (2) **lucos_repos `circleci` 07-30 07:15:59→07-31 07:13:19 = 23h57m red, blocking deploy-avalon.** Root cause verified from the job log (not inferred): pipeline 785 job `test` (2602) exit 1 on `proxy.golang.org` returning `http2: server sent GOAWAY ... debug="server_shutting_down"` fetching modernc.org/sqlite@v1.55.0 — upstream blip, not our code. Lasted 24h because (a) `go test ./...` has no retry and no GOMODCACHE cache so every build re-resolves the whole graph over the network, and (b) nothing re-runs a failed pipeline — it only cleared when the *next* morning's Dependabot merge (#477/pipeline 788) happened to run. Filed **lucos_repos#478** (suggest P3) with an explicit "1 occurrence in 20 pipelines, a WONTFIX is defensible" framing. **No re-run needed — CI already green** (repos v1.4.35 deployed 07:36:30Z).
- 2026-07-31 Check 2 minor/known: arachne `search` 07-30 07:33:38→07:33:46 = **3rd occurrence** of lucos_arachne#735 (07-02, 07-13, 07-30) — commented on #735 with the data point plus a NEW WEAK LEAD: it landed 29s after a `knowledgeIngest` "Knowledge graph updated" event and 4½min after the v1.3.39 deploy. Explicitly hedged as correlation-only: occurrence 2's nearest ingest was 4h36m away and ~45 ingests in 26d produced no flap ⇒ 1-of-3, contributing factor at best. Evidence erased again (arachne_search StartedAt 07-31 07:50) — 3rd consecutive time, which is the whole argument for #735. Also: docker_health `salvare-v4` 07-29 15:33→15:34 (1min, job ran 186s ago vs 180s threshold = known heartbeat blip); loganne `event-loop-lag-low` 07-31 07:11:41→07:11:54 (13s, p99 1826ms vs 1500ms threshold, mid-deploy-burst — the check is the post-#493 tuned version, working); backups `startup` 07-31 07:27:08→07:27:41 (33s, "startup in progress — data is being fetched", pure deploy artifact).
- 2026-07-31 Check 3 (30d, range 07-05→07-31): **0 incident reports needed.** Two NEW >30min entries, both correctly skipped. (1) **lucos_locations `location-freshness` 07-28 04:57:28→07-29 00:03:04 (19h05m alert, real gap ~49h06m)** — ⚠️ back-compute carefully: 108023s before the 07-28 04:57:28 onset = last data **07-26 22:57**, i.e. the phone reconnected at 07-26 22:57:04 (ending the previous alert), published for well under a minute, and went silent again immediately. **5th occurrence** of the same client-side `cheetah` pattern (07-15 31h42m, 07-18→19 18h sub-threshold, 07-20 1min, 07-26 32h25m, now 49h06m) and the longest yet — trending worse. Server exonerated as before; #91 caught it exactly as designed. Consistent with the 4 prior dispositions: NO FILE, NO REPORT, but **flagged to team-lead as informational** since it's lucas42's own device and the trend is the actionable part. (2) lucos_repos 07-30 24h = the CI-only outage above; repos.l42.eu served throughout, no user impact ⇒ issue not report. All other >30min entries pre-triaged: 07-16 22:39→07-17 00:07 CircleCI-API estate storm (~50 systems, upstream-only, 07-18 run); creds/docker_health 07-19→07-21 configy_sync key corruption (**report exists**: `docs/incidents/2026-07-19-configy-sync-key-corruption.md`); backups 07-05 (#365) + 07-07 (worlds bootstrap); repos 07-10 (#465); locations 07-15 + 07-26 (#91); lukeblaney_co_uk 07-14 (CI-only). The `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact (55/55 green proves it).
- 2026-07-31 Check 4 (5 containers, chosen as **oldest-reviewed that still had real log history** after the burst — a deliberate deviation from pure oldest-first, since a wiped buffer can't return a positive): lucos_contacts_db (42 lines, **0** errors, 23d), lucos_eolas_db (45 lines, **0** errors), lucos_worlds_db (21 lines, all the known-benign linuxserver TCP DB-readiness aborted-connection probes), lucos_aithne (488 lines since 07-28, **0** errors), lucos_locations_oauth2_proxy (238 lines since 07-10, **0** errors over 3 weeks — the 07-10 crash-loop remains fully resolved). 0 overdue @60d; the four @30d deferred as above.
- 2026-07-31: Monthly 5/6/7 NOT due (last 2026-07-05, 26d; due ~08-05).
- 2026-07-31 **⚠️ CORRECTION to this run's Check-2/Check-3 locations entry (and to 07-15/07-19/07-23/07-27)**: lucas42 asked for a ticket on the "recurring client-side `cheetah` publishing failure". Before filing I checked the claim against the recorder store — **and the premise collapsed**. `location-freshness` alerts are largely FALSE. Verified from `/store/rec/lucas/viper/2026-07.rec` using each record's `created_at` (recorder receive time, NOT the leading `.rec` column which is the fix `tst`): 07-15 alert reported 30.0h when the newest held fix was **47 min** old; 07-28→29 alert reported 30.0h→49.1h when the newest held fix was **3h29m** old and **18 more fixes arrived live during the alert window**; only the 07-26 alert was real (one genuine 32.4h stationary spell). July's true gap distribution for the device is **6–23h** (ordinary overnight/idle) with exactly ONE >30h gap — so the "worsening trend 31h→18h→1min→32h→49h" I reported was an artefact of trusting the debug string. Signature of the bug: `max(tst)` pins and then tracks wall-clock. Mechanism NOT established (check reads `/api/0/last` = recorder's `last` *cache*, a different path from the `.rec` store; **undiagnosable until #103 unbuffers info_server.py stdout**). **Filed lucos_locations#105** with the disproof, a verified alternative source (`/api/0/locations?...&limit=1&sort=desc`, carries both `isotst` and `isorcv`), the `tst`-vs-ingestion-clock design point, and an explicit "Correction to prior reports" section. Lesson saved as [[feedback_verify_check_claim_against_underlying_store]]: a check's debug string is a claim, repetition is not corroboration, and "it's the client / outside our control" is the conclusion that needs the MOST scrutiny because it ends the investigation.
- 2026-08-02 ops run: Monitoring **55/55 systems and 213/213 individual checks healthy** (0 failing/0 unknown), verified live at start AND end of run. Container count **57** on avalon. Check 2 (Loganne, fetched 411 events 07-26→08-02 = true 7d): 21 alert/recovery events, **20 of them pre-date the 07-31 run and were already triaged**. Only new one: **lucos_locations `location-freshness` 07-31 15:39:22→15:40:22 (1 poll)** — and it is a DIFFERENT debug string from the #105 staleness-claim family: `"Failed to fetch last recorded location data from the recorder"`, i.e. the fetch itself failed. Corroborated INDEPENDENTLY of the check's own claim, from the otfrontend access log: `/_info` was **835 bytes** at 15:39:22 vs 762/761 either side (+73 bytes = the failingChecks payload). And the diagnostic is still missing — otfrontend `StartedAt=07-31 07:25:20 restarts=0` so the buffer fully covers it, yet the only non-access-log line in the whole container is `Adding password for user lucas`. Textbook second confirmation of **lucos_locations#103** (info_server.py block-buffered stdout, Ready/Medium/owner=lucos-site-reliability) → **commented on #103**, no new file. ZERO alert/recovery events in the ~42h since. Check 3 (30d, range 07-06→08-02, 1976 events): **0 incident reports needed** — every >30min entry pre-triaged (07-16 22:39→07-17 00:07 CircleCI-API estate storm ~50 systems; 07-19→07-21 configy_sync key corruption, **report exists** `docs/incidents/2026-07-19-configy-sync-key-corruption.md`; backups 07-07 worlds bootstrap; repos 07-10 #465 + 07-30 #478; locations 07-15/07-26/07-28 (#91/#105); lukeblaney_co_uk 07-14 CI-only). `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact. Monthly 5/6/7 NOT due (last 2026-07-05, 28d; due ~08-05).
- 2026-08-02 Check 4 (8 containers — deliberately mixed: 4 with genuine multi-week history after the 07-31 estate burst, plus the four 07-01 stragglers deferred last run, reviewed against their ~2-day post-burst buffers rather than deferred a second time). CLEAN/benign: lucos_firewall (66,510 lines, **0** err-ish since 07-18), semweb (1931 = 100% `curl/8.7.1` scanner 404s probing error-logs/.env), lucos_mail_docs (8 = same scanner class), lucos_arachne_explore (5 lines, 0 errors), tfluke (142 = the known Scrapy-bot `Fetching National Rail data not yet implemented` route). **THREE NEW FILES, all from Check 4.**
- 2026-08-02 FINDING 1 → **lucas42/lucos_monitoring#294** (suggest Medium; repo had 0 open issues before, 111 total — positive control run after the empty search). **Every alert email lucos_monitoring sends is missing a `Message-ID:` header** (RFC 5322 non-compliant) and **Gmail has already bounced 2 of them, silently**. Parsed `lucos_mail_smtp` postfix log per queue-id since 07-13: 468 queue-ids with a message-id recorded, **466 EMPTY (`message-id=<>`) — all 466 from monitoring@l42.eu — 464 sent, 2 bounced**; only 2 non-empty. Bounces 07-20 20:19:01Z + 20:24:01Z: `dsn=5.7.1 550-5.7.1 Messages missing a valid Message-ID header are not accepted ... RFC 5322 ... - gsmtp`. Cross-referenced to Loganne, those two are the `lucos_docker_health` **recovery at 20:18Z and a fresh alert at 20:23Z — inside the configy_sync incident window** (sizes match: 658B recovery vs 1035B alert). Root cause verified from source: `src/email.erl sendEmail/2` composes only Subject/From/To/Date; relay does not compensate (`docker exec lucos_mail_smtp postconf always_add_missing_headers` → `no`, the postfix default). Proof it's the client not the relay: postfix's OWN bounce carries `message-id=<20260720201901.A604B4500095@avalon.s.l42.eu>`. **Silent by construction**: postfix accepted (queue-id assigned, 2xx to gen_smtp) so the `{error, send, {permanent_failure,...}}` clause never fired; the DSN went back to monitoring@l42.eu and Gmail took it with **`250 2.0.0 OK DMARC:Quarantine`** — the bounce notification went to spam. Second RFC-5322 bug on the adjacent line, flagged in the issue: `Date:` uses `calendar:system_time_to_rfc3339/1` (RFC **3339**), not RFC 5322 date format. Alternative/backstop noted but not filed separately: `always_add_missing_headers = yes` in lucos_mail.
- 2026-08-02 FINDING 2 → **lucas42/lucos_photos#500** (suggest Medium). `lucos_photos_worker` **shares SQLAlchemy pooled psycopg2 connections across `fork()`** → `psycopg2.OperationalError: insufficient data in "D" message / lost synchronization with server: got message type "0", length 775041074` on 07-31 17:08:30. Timeline proves the collision: 30.148 work horse forked for `generate_profile_picture`; 30.767 the **parent's sweep thread** ran `cluster_faces()` (DB); 30.902 the child desynced. Mechanism verified from source — `worker/app/main.py` starts `run_sweep_loop` as a parent daemon thread then calls `worker.work(with_scheduler=True)` (the file's own faulthandler comment documents "Worker.work() forks a work horse per job"), and `shared/lucos_photos_common/database.py` keeps `_engine` in a module global with default QueuePool, so `db.close()` returns a LIVE socket to the pool that fork then duplicates. `docker top` confirms parent+long-lived child. `pool_pre_ping`/`pool_recycle` do NOT help (socket is alive; timer isn't a fork barrier). **Threads are not the bug — QueuePool is thread-safe; it is specifically the fork.** RQ retried 10s later and the job succeeded, but the same shared socket can equally hand one process the other's rows with NO exception. Fix = one line: `os.register_at_fork(after_in_child=lambda: _engine.dispose(close=False))`.
- 2026-08-02 FINDING 3 → **lucas42/lucos_media_manager#283** (suggest Low; repo had 0 open issues before). **A 24-second TOTAL stall of lucos_media_manager, 2026-08-01 20:42:51Z→20:43:15Z**, with zero diagnostics. Established by extracting every `ceol.l42.eu` timestamp from the router access log — **59,699 requests over 50.4h (07-31 07:30:02 → 08-02 09:51:55), exactly ONE inter-request gap ≥12s**, and that's it. **Service-specific, not host/router**: total router throughput across all vhosts ran 1–5 req/s in every single second of the window (coincidence hypothesis killed with evidence). media_manager itself: `StartedAt 07-31 07:29:41 restarts=0 oom=false healthy`, RSS 143.8MiB, no container memory limit, and **zero log lines** 20:40–20:46. Runs as `java -cp manager.jar Manager` — **no JVM flags at all**, no GC/safepoint logging, no thread-dump-on-signal. Ask = the logging (`-Xlog:gc*,safepoint:stdout:time,level,tags`), NOT a fix — mechanism not established. Unverified lead recorded in the issue and explicitly labelled as such: `Status.waitForChange()` (Status.java:76) computes `this.hashCode()` (walks playlist/deviceList/collectionList) INSIDE `synchronized (changeLock)`, which `notifyChange()` also needs.
- 2026-08-02 seinn#583 corroboration (commented, no new file): 4 more `media-manager` probe failures since the 07-31 redeploy — 07-31 17:18:04 (822ms), 07-31 19:23:40 (800ms), 08-01 20:43:00 (**1637ms**, i.e. the abort fired at ~2x its 800ms deadline), 08-01 20:43:04 (800ms). None crossed `failThreshold: 2` so Loganne carries NOTHING about them — the numbers exist only because v3.js:68 console.warns them. **The caveat I left in #583's body ("proves media_manager responded, not that it responded fast") is now CLOSED**: the 08-01 pair sits inside the 24s router gap ⇒ it didn't respond at all. **TWO DISTINCT MODES, don't conflate**: (a) 07-31 17:18/19:23 = slow-but-successful (>800ms, no router gap); (b) 08-01 20:43 = total stall. Probe correct in both.
- 2026-08-06 ops run: Monitoring **54/55 systems, 212/213 checks healthy; 1 failing = `lucos_repos` `stale-dependabot-prs`** (verified live at start AND end of run, unchanged). Container count **57** on avalon. **Context: second estate-wide deploy burst in a week, 07:12–07:55Z, 27 `deploySystem` events**, driven by `lucos_loganne_pythonclient` **2.0.7** + `lucos_schedule_tracker_pythonclient` **2.0.9** publishing to pypi at 07:14/07:15 (patch bumps within 2.x, unlike the 07-31 major rollout). 36 of 57 containers restarted ⇒ Check-4 buffers wiped again ([[pattern_container_restart_log_buffer_artifact]]). **Post-burst whole-estate sweep (all 57 containers, since 07:00) = 9 hits, ZERO new**: mosquitto 605 (external TLS scanner noise on 8883), backups 24 (= BrokenPipeError, the known lucos_backups#374 :07 refresh-tracking block — verified still open, Ready/Low, deliberately NOT commented per the 07-19 "don't add still-happening noise" rule), media_manager 14 (LongPollController BrokenPipe), tfluke 11 (Scrapy National Rail route), otfrontend 3, and 4 singletons — arachne_search = benign Typesense raft `Peer refresh failed` 13s post-start, **router = a grep FALSE POSITIVE** (a media-api track URL containing "Feeling Exce**ption**al"). No breakage from the 2.0.x client patch bumps.
- 2026-08-06 Check 1 → **FILED lucas42/lucos_arachne#771** (suggest Medium). The single failing check is `stale-dependabot-prs`, red since 08-05 07:46 (re-alerted 08-06 07:22), debug names **lucas42/lucos_arachne#760** (`Bump mcp 1.28.1 → 2.0.0 in /mcp`, open since 08-03 07:14:56Z). PR is `mergeable_state: blocked`, auto_merge ALREADY enabled by lucos-ci[bot], 0 reviews — blocked purely by red CI. **Root cause verified from the job log, not inferred**: CircleCI `test` job (build 6340) step "Run MCP tests" exits 2 during collection, `server.py:29 → ModuleNotFoundError: No module named 'mcp.server.fastmcp'`; both test modules fail identically, every other job green. **Cross-checked against upstream release notes (v2.0.0, published 2026-07-28): "`FastMCP` is now `MCPServer`"** — a deliberate breaking rename ⇒ **deterministic, a CircleCI re-run can NEVER clear it** (same class of "don't re-run, migrate" as the lukeblaney_co_uk#68 exit-127 case). **NOT an outage**: arachne `main` CI fully green incl. `lucos/deploy-avalon`, deploys unblocked, `lucos_arachne_mcp` healthy on 1.28.1. Migration verified precise from the upstream migration guide: `from mcp.server.mcpserver import MCPServer`; `TransportSecuritySettings` path UNCHANGED; `streamable_http_app()` name unchanged but **transport params moved off the constructor onto it** (so arachne's `stateless_http=True` + `transport_security=` likely move — flagged as unverified in the issue); `@mcp.tool()`/`@mcp.resource()` decorators unchanged. Also flagged: v2 swaps httpx→httpx2 and TLS now uses the **OS trust store** not certifi ⇒ confirm outbound HTTPS from the container post-bump ([[pattern_scratch_image_no_ca_bundle]] class). Estate exposure CONTAINED: only lucos_arachne/mcp and lucos_comhra pin `mcp`, and **lucos_comhra is archived** (verified). Issue offers `mcp[cli]>=1.28,<2` (upstream's own holding pattern) as a legitimate alternative disposition — what isn't one is leaving #760 open and red.
- 2026-08-06 Check 2 (Loganne, fetched 1970 events, range 07-09 → 08-06 = 28d): only **3 distinct alert/recovery items since the 08-02 run**. (1) the repos `stale-dependabot-prs` alerts above. (2) **lucos_backups `startup` 08-04 07:30:41→07:30:54 (13s)**, debug "startup in progress — data is being fetched" = pure deploy artifact, identical to 07-31, benign. (3) **lucos_time `media` 08-04 14:47:20→14:48:04** → **FILED lucas42/lucos_time#348** (suggest Low/P3), see below.
- 2026-08-06 lucos_time#348 detail — **3rd instance of the estate-wide "probe discards its own diagnostic" pattern** (after lucos_media_seinn#583 and lucos_arachne#735, both verified still open). `src/server.js:141-148` does `fetch(testurl,{method:'HEAD',signal:AbortSignal.timeout(900)})` and on catch sets `debug = error.message`. TWO paths converge there: an explicit non-200 `Error` (informative) and a raw fetch rejection (undici's bare **`"fetch failed"`**, with the real reason discarded in `error.cause`). Observed debug was exactly `fetch failed`. `failThreshold: 5` confirmed live from `/_info` ⇒ ≥5 consecutive failed 60s polls before alerting, so the 44s alert→recovery span is the TAIL of a ≥5min real failure, not a 44s blip. Because `AbortSignal.timeout` raises a distinct TimeoutError message, "fetch failed" points at the network layer not the 900ms budget ⇒ **the alert was probably CORRECT; do not respond by raising the timeout** (hedged in the issue as Node-semantics inference, not reproduced). Fix = one line appending `error.cause.code`. 2nd firing seen in ops runs (prior 07-01 07:20).
- 2026-08-06 **⚠️ MY ROUTER-LOG PROBE WAS BROKEN AGAIN — caught it this time with a positive control.** Trying to corroborate the 08-04 `media` flap I grepped `lucos_router` logs for `staticmedia.l42.eu` in the window → **0 lines**, which reads exactly like "the requests never left the host". A positive control over the last 30 minutes ALSO returned 0, proving the probe not the absence. **Mechanism found:** `lucos_static_media` runs on **xwing**, not avalon (`lucos_configy` `config/systems.yaml`: `hosts: [xwing]`), and `lucos_time`'s `MEDIAURL=https://staticmedia.l42.eu/time` (verified from container env) ⇒ it is a CROSS-HOST probe over the public name and avalon's router never sees it. **Rule reinforced: avalon's router access log is NOT a universal estate oracle — check which host serves the vhost before treating a router-log blank as evidence.** This is also the substantive argument in #348: the check's own debug string is the ONLY record the request was ever made.
- 2026-08-06 Check 3 (30d, range 07-09 → 08-06): **0 incident reports needed.** Every >30min entry is pre-triaged — 07-16 22:39→07-17 00:07 CircleCI-API estate storm (~50 systems, upstream-only, 07-18 run); 07-19→07-21 configy_sync key corruption (~55 hourly docker_health cycles, **report exists** `docs/incidents/2026-07-19-configy-sync-key-corruption.md`); repos 07-10 (#465); locations 07-15/07-26/07-29 (#91 firings, but see [[feedback_verify_check_claim_against_underlying_store]] — lucos_locations#105 established most are FALSE staleness claims); lukeblaney_co_uk 07-14 (CI-only, site never down). One NEW entry: **2026-08-06 lucos_repos UNRECOVERED (started 07:22Z)** = the `stale-dependabot-prs` hygiene check above — repos.l42.eu served throughout, no user impact ⇒ issue (#771) not report. `:_circleci` UNRECOVERED lines remain the known humanReadable-parsing artifact.
- 2026-08-06 Check 4 (5 containers, chosen as **oldest-reviewed whose StartedAt PREDATES that review** — after the burst only ~10 of 57 had genuine coverage): lucos_scenes (43 lines, **0** err-ish since 07-27), lucos_contacts_db (15, **0**), lucos_eolas_db (6, **0**), lucos_locations_oauth2_proxy (158, **0** — 07-10 crash-loop still fully resolved, StartedAt 07-10 restarts=0), lukeblaney_co_uk (142,183 lines, 7 "err-ish" = **all scanner 404 probes matching on URL PATH text** — `/payment/error`, `/telescope/exceptions`, `/warn` — benign, same as 06-30/07-27). **DEFERRED, not silently recorded:** the 07-02 band (arachne_web, contacts_app, repos_app, eolas_app — now **35d, overdue @30d**) plus the 07-05/07-09 band all had buffers wiped at 07:30-07:46 today. They were covered by the whole-estate since-07:00 sweep (0 error lines each) but that is ~10h, not 5 weeks, so their tracking dates are left UNCHANGED and they take priority next run. **0 overdue @60d.**
- 2026-08-06 Check 5 (CI status, monthly — was 32d overdue): swept **71 non-archived repos** via CircleCI v1.1 `tree/main`. **ZERO repos red on main.** Probe validated by positive control (returns a genuine mix of `success`/`running`/`no-builds`, not uniform silence — the first pass filtered to nothing and I re-ran unfiltered rather than trusting an empty result). `lucos_schedule_tracker` showed `running` = an in-flight build, not a failure. NB arachne#760's red is on a PR branch, not main — main is green.
- 2026-08-06 Check 6 (/_info quality, monthly): swept **33 domains** derived from configy `config/systems.yaml`. **No defects.** Re-read `references/info-endpoint-spec.md` and corrected my own past framing: **Tier 1 is `system`/`checks`/`metrics`, and `checks`/`metrics` MAY be empty `{}` — only omission is a defect.** All 33 carry all three. So the long-standing "empty checks/metrics" list (configy, scenes, static_media, private, semweb, lukeblaney.co.uk, blog, tfluke + several `metrics: {}`) is **spec-conformant, not a defect** — worth remembering before re-flagging it. `lucos_worlds` omits `ci`, but `ci`/`title` are **Tier 2 (recommended)** ⇒ not a defect, AND **verified no monitoring gap: lucos_worlds HAS a `circleci` check** (monitoring sources the mapping elsewhere). Only 4 monitored systems lack a `circleci` check: `avalon`, `xwing` (hosts, not repos), `.github`, `vue-leaflet-antimeridian` — all legitimate. dns.l42.eu + dns2.l42.eu EMPTY BODY = known-expected (public host empty, monitoring polls internally, both healthy). **schedule-tracker.l42.eu returned a 502 mid-sweep → RE-PROBED rather than filed: 200/200/200, container StartedAt 17:10:50 restarts=0 healthy** ⇒ it was a deploy-window artifact (its CircleCI build was `running` at the time). Textbook case for re-probing before writing.
- 2026-08-06 Check 7 (external deps, monthly): Let's Encrypt **200**, Docker Hub **401**, CircleCI **401**, GitHub API **200**, docker.l42.eu mirror **401** — all nominal, nothing to raise.
- 2026-08-06: Monthly 5/6/7 now run (were 32d overdue, last 2026-07-05). Next due ~2026-09-06.
- 2026-08-08 ops run: **LIVE DEGRADATION FOUND — the day's headline.** Monitoring at start: 51/55 healthy, **0 failing but 4 "buffering"** (rotating set: configy/media_manager/media_metadata_api `fetch-info` timeouts + private `tls-certificate`). Buffering = failThreshold not yet crossed, so `failing: 0` HID an ongoing problem — **do not read `summary.failing == 0` as "all well"; read the per-system `status` field.** Loganne (2012 events, range 07-12 → 08-08 = 27d): 2026-08-08 is a massive outlier — `lucos_time` `media` **16 alerts** (vs 0–1/month), `lucos_media_seinn` `media-manager` 7, `lucos_static_media` `fetch-info` 2, `lucos_private` 2, `lucos_backups` `host-tracking-failures` 1. Storm onset **~00:00Z**, no deploy anywhere near it.
- 2026-08-08 ROOT CAUSE (two layers, both verified by live probe, not logs). **(1) External:** the home broadband link fronting **xwing** and **salvare** started dropping packets ~00:00Z. TCP connect (family 4, 80 samples each) from inside `lucos_time`: `152.37.104.10:443` (xwing via home NAT) p50 20ms / **p90 1027ms / 9-of-80 needing a SYN retransmit**; `178.32.218.44:443` (avalon public), `8.8.8.8`, `1.1.1.1` all **0/80**. Slow values cluster at **1028–1048ms and 3038–3065ms** = Linux SYN backoff (1s, 3s). Confirmed bidirectional: from a host ON the home LAN, avalon-public AND Cloudflare both showed 7/40 retransmits while xwing-local was 0/40 ⇒ the loss is the home WAN link, not the avalon↔xwing pair. Independent corroboration: `lucos_backups` logged **72 consecutive** `salvare.s.l42.eu: [Errno 110] Operation timed out` **00:09:20Z → 08:43:52Z**, reddening `host-tracking-failures` 00:14:28→08:49:19 (**8h35m**). ⚠️`ping` to 152.37.104.10 is USELESS (ICMP filtered → 100% "loss" on a healthy path); the 1.1.1.1 control caught that.
- 2026-08-08 ROOT CAUSE **(2) Ours — the amplifier → FILED lucas42/lucos#278** (suggest Medium). Node Happy Eyeballs gives the IPv4 attempt `autoSelectFamilyAttemptTimeout` = **500ms**, but the first SYN retransmit is at **~1s** — so one lost SYN becomes a hard `fetch failed` / `cause.code=UND_ERR_CONNECT_TIMEOUT` at a tight **508–517ms** mode instead of a 1s-slow success. Only bites when the target is **dual-stack** AND the container's docker network has no IPv6 egress (`lucos_time_default`/`lucos_monitoring_default` = EnableIPv6 false ⇒ `ENETUNREACH` in 3ms). **A/B during a live burst, interleaved, n=70: default 500ms → 29 fails (41%); 3000ms → 3 (4.3%); autoSelectFamily=false → 4 (5.7%)** ≈ 8x amplification over ~5% underlying loss. Which domains are dual-stack is an ACCIDENT OF DNS AUTHORING (AAAA iff CNAME to `*.s.l42.eu`): staticmedia/am/media-api YES; ceol/seinn/configy/monitoring/private NO. Issue offers 3 options (bump attempt timeout / give the networks IPv6 egress / drop the AAAAs) and deliberately does NOT pick one — spans DNS + docker networking + every Node service. See [[pattern_happy_eyeballs_amplifies_syn_loss]]. Also **commented on lucos_time#348** — its one-line `error.cause.code` fix is exactly what would have short-cut this; and my earlier "don't raise the 900ms timeout" call is now confirmed (failures aborted at ~510ms inside connect, the 900ms budget was never binding).
- 2026-08-08 ⚠️**BURSTY-PHENOMENON TRAP, nearly fell in twice**: failure rate swung 0% ↔ 41% within minutes. Two A/B runs returned **0/120 and 0/60** and would have "disproved" a real, ongoing fault. Fix: interleave all arms inside ONE loop so every arm sees identical conditions, and check the alert duty-cycle before concluding "not reproducible".
- 2026-08-08 **HOST FINDING → killed a 108-day runaway.** avalon PID 985893, user `lucos-agent`, PPID 1: `bash -c 'docker run … & sleep 3 kill %1 … wait'` — a malformed agent SSH one-liner from **2026-04-22** (missing `;`/newlines made `kill`/`wait` arguments to `sleep`) pinned at **99.7% CPU with 107 days of CPU time**, i.e. a full core of a 4-core host, with a `<defunct>` docker child. Nothing alerts on it: host CPU isn't a monitoring check and a runaway `bash` isn't a container crash. Killed (baseline captured first per production-change-verification): **1-min load 2.60 → 1.21**, monitoring 53→54/55 healthy. Prevention added to `references/ssh-production.md` §Connecting via SSH (never `&` inside an `ssh host "…"` one-liner; use `timeout N` in the foreground). xwing swept, clean. See [[pattern_orphaned_ssh_background_job]].
- 2026-08-08 Check 3 (30d, range 07-12 → 08-08): **0 incident reports written — 1 DEFERRED, deliberately.** The only new >30min entry is today's `lucos_backups host-tracking-failures` 8h35m, which is the same home-link fault and **still live at report time** ⇒ per the "ongoing impact → don't write yet" rule, a report is owed once the link recovers. Everything else pre-triaged (07-16 CircleCI storm; 07-19→21 configy_sync key corruption, report exists; repos/locations/lukeblaney_co_uk entries). `lucos_repos stale-dependabot-prs` **recovered 08-07 11:39Z** — lucos_arachne#771 and Dependabot #760 both now CLOSED, so last run's finding is resolved.
- 2026-08-08 Check 4 (5 containers): `lucos_time` and `lucos_backups` reviewed exhaustively as part of the investigation above. Plus the four 37-day-overdue 07-02 stragglers (arachne_web, contacts_app, repos_app, eolas_app) — all restarted in the 08-07 07:1x–07:4x deploy burst, so their buffers cover only ~28h, but that is 100% of the CURRENT container's life, which is the best coverage obtainable; **recorded as reviewed rather than deferred a second time** (the 07-02→08-07 gap is permanently unrecoverable). All four: **0 err-ish lines** (arachne_web 11,956 lines, contacts_app 210,805, repos_app 91, eolas_app 23,912). 0 overdue @60d.
- 2026-08-08 Monthly 5/6/7 NOT due (last 2026-08-06, 2d; next ~2026-09-06).
- 2026-08-08 LINK RECOVERED + incident report. Last alert 10:52:35Z→recovery 10:53:30Z (alerting window 00:00:56→10:53:30 = 10h53m), but **loss outlasted the alerts**: my own probes still showed 8/40 retransmits at ~12:00Z and only went clean at ~12:41Z (0/50 avalon→xwing; 0/40 home→avalon AND home→Cloudflare, i.e. both directions). ⚠️**Alert cessation is NOT fault clearance** — checks are probabilistic, so a partly-lossy link stops crossing failThreshold long before it's healthy. Verify recovery by direct measurement, not by the alert feed going quiet. Monitoring 55/55 healthy 0 unknown at 12:41Z. Final alert tally for the day: **29 monitoringAlerts** — lucos_time `media` 17 (00:09:30→10:52:35), seinn `media-manager` 7, static_media `fetch-info` 2, private `fetch-info`/`tls-certificate` 1 each, backups `host-tracking-failures` 1. **24 of 29 (time+seinn) are Node ⇒ amplifier-attributable; the other 5 are Erlang (`fetch-info` = monitoring's own `src/fetcher_info.erl`) and Python/Fabric ⇒ ordinary loss.** Report drafted + **draft PR lucas42/lucos#280** open, six domain teammates notified. Follow-ups all pre-existing: lucos#278 (Ready), lucos#279 (Ready, sysadmin), lucos_time#348. **Deliberately NOT filed, argued in the report**: an external-link check (can't fix or action an ISP fault) and an alert-rate anomaly detector (would have caught it hours earlier, but #278 removes 24 of 29 alerts at source — revisit with evidence if a storm recurs after that lands).

