---
name: incremental-rsync-container-proxyjump-hostkey
description: lucos_backups incremental (rsync --link-dest) path had TWO serial latent bugs surfaced by the photos seed — #327 host-key (#329) and #330 publish-step && (#331); BOTH FIXED, photos_photos GREEN as of 2026-06-14 (v1.1.15)
metadata:
  type: project
---

The lucos_backups `incremental` backup_strategy (ADR-0002, #324) runs rsync inside a fresh `docker run` container **as root with an empty known_hosts** (`/root/.ssh` doesn't even exist in `lucas42/lucos_backups` image). `host.py::_container_ssh_command` sets `ssh -o StrictHostKeyChecking=no -o ProxyJump=lucos-backups@xwing…`, but that flag covers only the **outer** hop (→aurora) — it does **NOT** cover the **ProxyJump hop to xwing**, which then fails host-key verification.

**Diagnostic signature:** rsync exit 255 + `Host key verification failed` + `Connection closed by UNKNOWN port 65535` + `connection unexpectedly closed (0 bytes received)`. Note: with `StrictHostKeyChecking=no` set, an unknown *target* key is auto-accepted — so a host-key failure WHILE that flag is set means a hop the flag isn't reaching (the jump). The old full-snapshot host-side scp path works because it runs as the lucos-backups OS user whose known_hosts trusts xwing+aurora; the container has neither.

**Status (2026-06-12):** `lucos_photos_photos` is flipped to `incremental` (configy#230 live) and v1.1.13 is deployed, so the path is LIVE and BROKEN. The nightly create-backups cron (03:25/15:25 UTC) routes photos through it and fails identically → `create-backups`/`host-tracking-failures` red for `lucos_photos_photos`, one re-alert per run. **This is NOT a new outage** — the old full-snapshot path was already failing on this volume (600s scp timeout, [[pattern_backups_invalid_effort_crashes_host_tracking]] sibling #309). Discovered running the off-cron seed (`scripts/seed-volume.py lucos_photos_photos`) — the seed gate worked as designed, catching it before lucos_photos#427's big import.

**UPDATE 2026-06-14 — #327 FIXED, but a SECOND bug (#330) now blocks photos.** #329 (merged, v1.1.14) fixed the host-key issue by mounting the source host's `/home/lucos-backups/.ssh/known_hosts` read-only into the rsync container (true parity with host-side scp; chosen over ProxyCommand because rsync `--rsh` splits on whitespace and would mangle a space-containing ProxyCommand). Verified live: rsync now transfers (seed + nightly cron both land data in `<date>.partial/` on aurora). **But the seed STILL fails** — the atomic-publish step `runOnRemote(target, 'rm -rf <final> && mv <partial> <final>')` sends the command to ssh UNQUOTED (`host.py::runOnRemote` builds `ssh {} {} {}`), so the `&&` is parsed by the LOCAL shell → `rm -rf` runs on aurora but `mv` runs on AVALON (where `.partial` doesn't exist). **Tell: the `mv` error comes back in avalon's Irish locale** (`ní féidir … a stat`), not the QNAP's English. Data is stranded in `.partial`, never published. Line 253 is the only compound-command `runOnRemote` caller; fix = `shlex.quote` the command in runOnRemote. Tracked **lucos_backups#330** (P2, open).

**RESOLVED 2026-06-14:** #331 (shlex.quote in runOnRemote, v1.1.15) fixed the publish step. Verified: seed published a real atomic `2026-06-14/` snapshot on aurora (3448 files), stranded `.partial`s cleaned up, then a manual full `create-backups` run completed (Backups Complete, exit 0) — photos rsync+publish succeeded in the real job, no bug #3. Monitoring: estate 54/54 healthy; all 16 lucos_backups checks healthy; `create-backups` flipped healthy off the manual run. `lucos_photos_photos` is GREEN. Steady state OK: nightly cron now finds `2026-06-14/` as prior → `--link-dest` dedup works.

**Incident report (merged 2026-06-14):** `lucos/docs/incidents/2026-06-08-photos-volume-backup-gap.md` (PR lucos#245) covers this whole saga — Check-3 Incident-Coverage is satisfied, do NOT re-flag as missing. Two follow-ups came out of review, both open: **lucos_backups#332** (security: `StrictHostKeyChecking=no`→`accept-new` to reject *changed* keys, not just verify known — P3 hardening) and **lucos_backups#333** (architect: orphaned `<date>.partial/` dirs never GC'd → unbounded count on capped aurora; owner=me, Low; my posted design = clean stale `.partial` at START of each run, `date≠current-run` predicate so same-date resume is untouched). ADR-0002 rollout amendment = architect's **lucos_backups#334** (references #333). NB this incident did NOT actually leak a `.partial` (#330 fixed same-date, seed resumed-and-published same-date).

**How to apply:** photos_photos is GREEN — no longer a known-red. The incremental path is sound. **Durable lessons:** (1) `StrictHostKeyChecking=no` does NOT propagate to the ProxyJump hop — give the container the host's known_hosts (mount) rather than fighting jump-hop quoting; rsync `--rsh` also splits on whitespace so a space-containing ProxyCommand is a trap. (2) `runOnRemote` compound commands must be `shlex.quote`d or the local shell eats the `&&`/`;` — tell is a SOURCE-locale error (Irish on avalon) for a TARGET-path operation. (3) **A backup path you can't exercise in CI hides bugs in SERIES** (matches 2026-04-28 aurora incident) — always run ONE clean end-to-end after each fix. (4) The `create-backups` MONITORING check is job-level + schedule_tracker-fed: the ad-hoc seed does NOT clear it; only a successful `create-backups` RUN (cron or manual `python -m scripts.create-backups`) does. (5) monitoring `/api/status` checks use `status` field (healthy/failing), NOT `ok`; per-system `healthy` field is unused (always None) — trust per-check `status` + the summary counts. The verified-RESTORE half of lucos_photos#427 remains separate/open.
