---
name: aithne-migration-guide
description: lucos_aithne consumer-migration guide (#159) and the three decisions gating it
metadata:
  type: project
---

**✅ MIGRATION COMPLETE / `lucos_authentication` DECOMMISSIONED 2026-06-29** — all consumers off; lucos_aithne#12 + lucos_authentication#143 both closed/completed. Below is historical record of how the consumer migration + guide were driven; references to `lucos_authentication`/`auth.l42.eu` are the retired system aithne replaced.

**Pre-broad-migration review (2026-06-24, team-lead asked before kicking off the rest of the estate; arachne = done reference). VERIFIED live:** prod aithne `/_info` all-green + JWKS serving; **arachne canary is §6-CLEAN** — `mcp/server.py:_has_arachne_access` gates `/explore`+`/mcp` on `arachne:read in scopes` for ALL principals (interim principal_class-only defect fixed) = safe reference. **Human-session continuity LANDED 2026-06-23: aithne#181 (IdP session+re-mint) + navbar#174 (keepalive) both CLOSED;** merged guide pins `lucos_navbar >= 2.2.0` + documents the sequencing gate → the 15-min-reauth blocker is CLEARED. Guide #186 merged/complete (AITHNE_ORIGIN, dev-against-dev-aithne). **Only open aithne issue = #12.** The ONE gating pre-req before any code-wave = **the holistic scope-design pass** (§6: every consumer UI on a granted+checked scope; vocab today only has arachne/eolas/media-metadata; ~8 others — notes/seinn/creds/loganne/comhra/backups/photos/contacts — have NO UI scope). lucas42's 2026-06-16 batch decision = one estate vocab PR + one aithne redeploy BEFORE wave 1 (grant-before-enforce). Was only folded into #12 umbrella → **raised discrete ticket lucos_auth_scopes#19** (mine to drive; awaiting lucas42 go-ahead). Reported to team-lead.

**SPLIT after lucas42 review (2026-06-22):** he approved ADR-0003 but requested 3 changes on the bundled guide → split. **ADR-0003** (human session continuity, supplements ADR-0001 §3) = **MERGED/Accepted 2026-06-22 (PR #182, closed #147)**. lucos-security APPROVED w/ impl observations cross-ref'd onto #181 (POST verb, CSRF-harmless-document-in-code, CORS explicit-allowlist-not-glob, principal-revalidation-HARD-sync, runbook #162 needs IdP-session-invalidation step) + navbar#174 (one-remint-per-interval anti-race, pause-on-document.hidden). **Guide** (`docs/consumer-migration-guide.md`, #159) = **MERGED 2026-06-22 (PR #186, closed #159; lucos-security APPROVED no findings)**. Pending hardening (drive-by, no vuln, awaiting route): add `next=` use-current-request-path nudge to redirect step — batch with navbar#174 version patch. Originally draft PR #186, revised per his 3 comments: (1) BULK not per-service framing, (2) machine-keys dropped (no lucos_authentication equivalent → nothing migrates; also dissolved prod-secret hand-off as human-session verify needs no secret), (3) explicit navbar version not "current". **#186 open item: exact navbar version can't be named until navbar#174 ships — offered lucas42 hold-vs-land-now-patch-later.** Deferred impl: **aithne#181** (IdP session + re-mint endpoint) + **lucos_navbar#174** (navbar keepalive + multi-tab; Blocked on #181). See [[machine-principal-sessions]].

**How to apply:** #182 closes #147, #186 closes #159. The three (settled) gating decisions, for reference:
- **lucas42/lucos_arachne#657** — ✅ DECIDED (lucas42) **Option 1**: per-service access-denied page. Three-branch auth middleware (proceed / consumer's own styled 403 via its error view / login-redirect). **No** shared aithne request-access endpoint. Non-negotiable: stop redirecting wrong-scope→login, return 403. Captured on #159.
- **lucas42/lucos_aithne#147** — ⏳ human session continuity (15-min token TTL). lucas42 REJECTED per-form approaches (Axis 2: silent-refresh-before-submit/stash-replay/draft-autosave) — wants central-only or he'll lengthen sessions to hours/days. **My central counter (recommended): session-keepalive in the SHARED NAVBAR** (`lucos_navbar.js`, verified loaded estate-wide via import/script-include) — background timer + focus handler silently re-mints the short `aithne_session` via a new aithne re-mint endpoint backed by a long-lived IdP session; while a tab is open the token never expires mid-form → no redirect, no lost POST, ZERO per-form changes (only the navbar bumps). Optional 1 global `submit` listener in navbar closes wake-from-sleep race. **Security reconciliation: longer STATELESS token = hours/days revocation lag** (consumers verify JWT locally via JWKS, scopes baked at mint) — real regression on crown-jewel; longer sessions safe only if revocable, and clean revocable design IS IdP-session+short-token+keepalive (paths converge). Needs 2 new central aithne capabilities + navbar change + #148 cookie/CORS. Best as ADR-0001 supplement. Awaiting lucas42 pick: keepalive vs longer-sessions.
- **lucas42/lucos_aithne#148** — ✅ DECIDED (2026-06-17), both parts. Part 1: consumers inject `AITHNE_ORIGIN` env var (not hardcoded `https://aithne.l42.eu`); doc in local-verification-contract.md. Part 2 (lucas42 +1): dev consumers verify against **dev aithne instance**, not prod (cookie scoping). Affects whether client-side silent-refresh (#147 option A) is possible (SameSite/Secure/Domain cookie scoping). Captured on #159.

**Re-mint CORS = `*.l42.eu` glob, NOT per-consumer registration (lucas42/lucos_aithne#191, merged 2026-06-23).** Supersedes the line-14 security note "CORS explicit-allowlist-not-glob": #191 re-scoped it — `remint.go` `setCORSHeaders`/`l42euOriginRe` accepts any `https://*.l42.eu` origin; `ListAllowedCORSOrigins`/the `oidc_clients`-derived allow-list is GONE. So a JWKS-local consumer needs NO CORS/OIDC-registration step for keepalive. **Do NOT re-introduce per-consumer OIDC-client registration for CORS** — it re-adds the fake-`redirect_uri`/dead-`client_secret`/OIDC-RP attack surface arachne#676 rejected. (Caught a stale P2b proposing exactly that in migration-doc-gaps ticket #198 → PR #203; #198 was filed by code-reviewer AFTER #191 closed, carrying the dead arachne#676 premise. Lesson: a "discovered requirement" from a PR may already be superseded — check before documenting it.)

Doc-gap ticket **lucas42/lucos_aithne#198** (filed 2026-06-23 by code-reviewer): 4 migration-doc hardening reqs from the arachne migration → my PR #203 (draft, awaiting lucas42). Items 1–3 correct (ES256 algorithm-pinning §3, `kid` C0+DEL log-sanitisation §2, distinct WARNING-level JWKS-fetch-failure logging — from arachne#637/#646/#641). Item 4 (P2b OIDC-CORS-registration) dropped per above.

Related doc tickets on aithne: #156–#159 (local-verification-contract.md gaps + the migration checklist).

**Django consumer reference design = lucos_eolas ADR-0002 (2026-06-24, PR lucos_eolas#319 — MERGED at commit 68e0af9, merge 05b68e2).** Drove lucas42/lucos#249 (design ticket, lucos repo). **MERGED-HEAD CAVEAT:** main reflects `68e0af9`, which handles `principal_class` at **§5 (mapping hook)** and **KEEPS** the render-ui agent-Bearer path (code-reviewer's placement, lucas42-approved). lucos-security's CHANGES_REQUESTED (its literal ask = a **hard `principal_class=="human"` reject in §3**) was on the ORIGINAL `ae9ee6b` and was **never re-reviewed on the merged head** — the code-reviewer integrated it at §5 on security's behalf and merged. My follow-up `7768b53` (§3 hard gate + render-ui DROPPED) was pushed **after** lucas42's approval auto-merged 68e0af9 → **NOT in main, orphaned**. Post-merge: flagged to team-lead; §3-gate-vs-render-ui is mutually exclusive and a lucas42 design call (render-ui was his ask); a follow-up amendment ADR/PR can carry 7768b53's content if he wants the §3 gate. **Follow-up tickets filed 2026-06-24 (board placement = team-lead):** vocab lucas42/lucos_auth_scopes#21 (add eolas:admin/contacts:read/contacts:admin, retire contacts:use; build-time coupled, §7 approval); eolas migration lucas42/lucos_eolas#321 (reference impl); contacts migration lucas42/lucos_contacts#753 (follows). **Security follow-up = lucos-security's lucas42/lucos_eolas#320** (NOT mine — don't dup): §4 "valid token" ambiguity, Option A (clarify §4 → @require_scope checks is_authenticated AND scope; agents map to AnonymousUser so prod hard-stops them; render-ui survives via dev bypass) vs Option B (7768b53 hard §3 human-only, render-ui dropped). **I recommended Option A** (closes hole AND keeps render-ui; is_authenticated is idiomatic Django) — decision reduces to lucas42's render-ui yes/no. **Option A amendment = PR lucas42/lucos_eolas#322** (still DRAFT, `Resolves #320`, #320 Blocked-on-#322/Owner-me auto-closes on merge). **lucas42 CHOSE OPTION A 2026-06-24** with TWO requirements → PR revised to **commit d2054c4** (supersedes aa56673, which both bots had approved): **(req 1)** dev-only render-ui now REACHES the Django admin of both services — §5 maps a dev render-ui agent to a **dev-only staff principal** (is_staff/is_superuser), GET/HEAD-only, strictly ENVIRONMENT==development-gated, inert in prod (revises earlier "render-ui never reaches admin", for dev only); **(req 2)** keep door open for a FUTURE prod agent scope (planned human-approved data-suggestions feature) — agents verified+scopes stashed (§3), a future dedicated agent endpoint authorises on principal_class==agent + dedicated scope (e.g. eolas:suggest), NEVER a human-UI scope; not built now. Prod security property unchanged (agent→AnonymousUser in prod; is_authenticated gate hard-stops wrongly-granted human-UI scope). Review iteration: d2054c4→code-reviewer flagged GET/HEAD enforcement-location gap→d315642 (middleware-405)→security REQUEST_CHANGES prescribing the check live in **map_principal** (mint dev staff principal ONLY for GET/HEAD, else fall back to AnonymousUser; NOT decorator/NOT CSRF)→adopted theirs = **commit 26e38c5**. **Both bots APPROVED on 26e38c5** (security 12:32Z, code-reviewer 12:36Z — verified against review API on the actual head, not a relay). #322 marked ready→**lucas42 CHANGES_REQUESTED: rework to SCOPE-FIRST** (don't gate access on principal_class — decide on scopes; principal_class only for contact-matching/navbar; and stop accreting ADR text each round — see [[feedback_scope_first_not_principal_class]]). **Reworked to scope-first** (net −90 lines): §4 clean three-branch (access=scope, default-deny ADR-0001 §6 is the protection), §5 map_principal uses class only for identity, dev render-ui concise (dev-only/read-only GET-HEAD/reaches admin; method check in map_principal not @require_scope). Removed the is_authenticated-barrier/two-barrier/agent→AnonymousUser edifice. **Head now 2efd05a** (bcd74c6 + 1-line map_principal-vs-@require_scope clarifier). **Both bots APPROVED 2efd05a** (security 12:51Z — NO pushback on removing the is_authenticated barrier, §6 satisfied them; code-reviewer 12:52Z — verified via API). **#322 MARKED READY + lucas42 RE-REQUESTED** (his CHANGES_REQUESTED was on superseded 26e38c5; submitting it cleared his review-request so I re-added him via POST requested_reviewers). **Awaiting lucas42's re-review** (team-lead relays; supervised→his approval auto-merges). On merge: `Resolves #320` auto-closes; migrations #321/#753 unblock behind vocab #21. (Earlier lesson still holds: ADR/design PR lands independently of the code migrations.) **LESSON (team-lead flag): when a choice is routed to lucas42 as Awaiting Decision, default to HOLDING implementation; a reversible DRAFT PR is the most to put up in that window (it can't merge + he still gates) — a non-draft PR would pre-frame his decision.** Did NOT open the 7768b53/Option-B PR unprompted (code-reviewer suggested it, but it reverses lucas42's approved keep-render-ui). No live vuln (design doc, no code migration written yet). **PROCESS LESSON: on a SUPERVISED repo, lucas42's approval auto-merges within SECONDS too** — pushing a security fix after his approval races the merge exactly like the unsupervised case; an outstanding security CHANGES_REQUESTED on a superseded commit did NOT block auto-merge (branch protection gap — surfaced to team-lead). Verify PR still open before assuming a late fix lands. Covers BOTH Django session consumers (eolas + contacts); eolas=reference, contacts follows (only `map_principal` + scope grain differ). Home = reference repo's own docs/adr (NOT estate lucos docs/adr — estate model is aithne ADR-0001). lucas42's 3 #249 questions settled: (1) evaluated off-the-shelf OIDC plugins (mozilla-django-oidc etc.) → REJECTED for same-site services: they all imply path-(b) OIDC-RP + bridged Django session (re-adds revocation staleness + per-service client_secret); crypto is PyJWT either way → **path (a)** shared-cookie + per-request local verify, no bridged session; (2) AnonymousUser is standard Django contract, navbar name does NOT break (contacts navbar reads `agent.getName()` from local Person, not JWT); (3) **scope-driven is_staff/is_superuser** replaces hardcoded id==2. **Per-service scope grain** (his readonly-vs-admin hint): eolas admin-only → `eolas:admin`; contacts readonly-layer + admin → `contacts:read` + `contacts:admin`. **eolas IS SUPERVISED** (check-unsupervised exit 1) → normal PR + lucas42 auto-reviewer (NOT draft). Mechanism: populate-only middleware (sets request.user, defaults AnonymousUser, never blocks → `/_info` exempt via per-view enforcement, Django-idiomatic equiv of route-order rule); serve-last-known-good wrapper for PyJWKClient raise-on-fetch; three-branch authz (C2); CSRF already covered by Django CsrfViewMiddleware. **Follow-ups flagged to coordinator:** (a) NEW lucos_auth_scopes vocab PR (add eolas:admin/contacts:read/contacts:admin, retire contacts:use) — gating both migrations; (b) per-consumer eolas+contacts migration tickets (none found; part of aithne#12 waves W3/W4).
