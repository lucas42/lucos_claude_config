---
name: machine-principal-sessions
description: Auth design for lucos_aithne (formerly lucos#132) — the new passkey auth service; machine principals session-capable not bearer, plus the full OIDC/passkey design proposed 2026-06-09
metadata:
  type: project
---

# lucos_aithne auth design (formerly lucas42/lucos#132)

**Ticket moved 2026-06-09:** lucos#132 was transferred to its own repo and is now **lucas42/lucos_aithne#1** (the founding tracking issue). Service named **`lucos_aithne`** — Irish for *recognition / knowing a person* (names the function, not the passkey mechanism; distinct from `lucos_authentication` for safe coexistence during migration). priorities.md already updated (origin/main): aithne is active priority #1, firewall complete/retired.

**scopes.yaml divergence (#52, 2026-06-10):** lucas42 flagged aithne's in-repo `scopes.yaml`. Verdict: in-repo file is unavoidable (`//go:embed` needs a compile-time file); prod never diverges (Dockerfile COPYs canonical from `lucos_auth_scopes@<digest>` 1.0.3); but the CircleCI `test` job runs `go test` against the STUB while the image embeds canonical, and NOTHING in CI reconciles them — "in sync" is manual-discipline only (scope strings identical today: render-ui, aithne:admin). Build-time coupling (ADR §7) is sound, keep it. **I recommended a CI drift-check; lucas42 OVERRULED → remove the stub entirely.** His call was better (removes the whole drift surface, not just the symptom). I then proposed Option 2 (drop embed, read at startup); **lucas42 chose Option 1 instead — KEEP `//go:embed` (vocab in the binary), remove only the committed stub.** His reasoning (right): for the most security-sensitive binary, vocab-in-the-self-contained-static-binary (no runtime file dep, no startup failure mode) beats a startup read. **Final design (ADR-0001 §7 amendment, PR #55 MERGED 12:47Z sha 8616b87):** keep embed; `scopes.yaml` gitignored + fetched from canonical before EVERY compile — image build via `COPY --from=scopes`; CI test job + local via `docker create`+`docker cp` from the **scratch** `lucos_auth_scopes` image (NOT `docker run cat` — scratch has no shell); `//go:generate` for local; CI shape = small `fetch-scopes` job → `persist_to_workspace` → `test` attaches. **main.go UNCHANGED.** Impl spec on **#52** (build/CI plumbing only; →developer; security reviews). **Lesson: when a sync-drift risk appears, first ask if the redundant copy can be DELETED, not how to keep it in sync.** creds doesn't consume vocab yet (#375 open). **REUSABLE Dependabot-pinning fact (verified vs dependabot-core, 2026-06-10):** docker ecosystem parses `FROM` ONLY — NOT `COPY --from` (open gap #5103); bumps a pin only when a **tag+digest** are both present (digest-only tracks `latest`, no semver); `ARG`-in-`FROM` unsupported (#4597 not_planned) and converting a literal FROM→ARG makes Dependabot drop the dep & close PRs. So to keep a COPY-from'd pinned image Dependabot-tracked: alias it as `FROM img:tag@digest AS stage` + `COPY --from=stage`. (My earlier `ARG SCOPES_IMAGE` idea was wrong on all counts — lucas42's "will dependabot bump it?" caught it.) **Estate audit 2026-06-10 (lucas42 asked, off lukeblaney_co_uk#63):** code-search + local cross-check across 93 repos → only 2 ever-affected = aithne(#52 done) + lukeblaney_co_uk(#63 handled); ARG-in-FROM=0; `docker-image://` buildx contexts=0. **Gotcha: `lucos_configy` `COPY --from=config` is NOT an image — `config` is a local-dir `additional_contexts` mapping (`config=config`); my classifier flagged it, manual check cleared it.** No new per-repo issues. Raised forward-prevention detector **lucos_repos#416** (silent failure + security-relevant + growing shared-image idiom justify a detector over a one-off sweep).

**Bootstrap-admin first-passkey enrolment (#48, 2026-06-10):** ADR-0001's "recovery=admin-re-enrolment-only" left a chicken-and-egg — no defined way to seed the FIRST admin's passkey (admin surface issues invites→needs passkey→needs invite). Designed + lucos-security-signed-off. **ADR-0002 = MERGED/Accepted** (PR #50, lucas42-approved 11:09Z, merged 11:19Z, merge sha 2a556dc; review caught Proposed→Accepted status + a #48→#49 ref) + runbook on main. Code = **ticket #49** (owner=developer; team-lead dispatching; security to review code). Decisions: (1) first passkey via one-shot `--bootstrap-invite` BINARY SUBCOMMAND not HTTP endpoint — docker-exec/host access already aithne-admin-equiv (SIGNING_KEK/DB-write) so zero new capability + no network surface; HTTP would reopen the lockout security relied on; refuses any contact≠BOOTSTRAP_ADMIN_CONTACT_ID. (2) `bootstrapAdmin` self-disables on credential-gate (≥1 webauthn cred → no-op) — fixes verified bug: `idx_grants_active` is PARTIAL (`WHERE revoked_at IS NULL`) so a *revoked* aithne:admin grant silently RESURRECTS on restart; credential-gate (not principal-gate) keeps catastrophic recovery e2e. (3) startup WARNING while active + runbook step to unset env var post-enrol. **Finding flagged:** `BOOTSTRAP_ADMIN_CONTACT_ID` is in dev creds but MISSING from compose `environment:` array → never reaches container → bootstrap inert as deployed (PORT = working counter-example: bare-list passthrough needs the var named); 1-line fix in #49.

**Design SIGNED OFF by lucas42 2026-06-09. ADR-0001 drafted + tickets raised.** Spine: self-hosted **OIDC OP**; humans→WebAuthn passkeys (RP ID = **`l42.eu`**, not the subdomain, so login origin can move); machines/agents→OAuth2 client-credentials (key in creds, session minted at runtime never stored). Both yield the **same short-lived signed JWT** (principal-class + identity + scopes), cookie on `.l42.eu`, **verified LOCALLY via JWKS** (replaces `/data?token=` introspection — also the structural answer to lucas42's centralise-rate-limiting ask: remove the per-backend check, don't add a central checker). Cross-iframe SSO works because all services are same-site under l42.eu.

**lucas42's sign-off decisions + refinements (the bits that differ from my proposal):**
- **Full OIDC from day one** (firm, not later) — for off-the-shelf tools AND any system on a separate domain. Shared-JWT path is *additional*, not a replacement.
- **Account recovery = admin re-enrolment ONLY** (no ≥2-passkeys, no recovery codes), and **enrolment + recovery are the SAME admin-invite flow**.
- **Third-party token brokering split out ENTIRELY** — aithne is identity-only. Audit ticket for impact (he expects little; case-by-case).
- **Credential↔contact: aithne-owned. AI agents get NO lucos_contacts entry. Humans MUST be lucos_contacts** ("if not a contact, why give access"). **aithne mints NO identities of its own (architectural stance, lucas42 2026-06-09)** — every principal references an external authority; aithne owns only the credential. THREE authorities: lucos_contacts(humans) / agent-registry(AI agents) / lucos_configy system-codes(services). **Services (lucos_root) are NOT aithne-native** — short-term stay on lucos_creds linkedCredentials (not aithne principals at all); long-term IF migrated → identified by configy system code, never minted. (Supersedes older "machine resolves to a service-account contact ID" AND my earlier "services aithne-native" lines.) Day-one principal classes = human + AI-agent only. **Agent principal id = the `lucos_agent` `personas.json` slug** (e.g. `lucos-architect`) — *referenced not minted*, captured at provisioning, NO runtime/build dep on personas.json (it's ad-hoc + to-be-replaced — replacement parked on lucos#155 bespoke-harness ideation w/ slug-stability constraint recorded there; only contract = slug stability). Symmetric w/ humans: identity authority always external (contacts / agent-registry), aithne owns only the credential. (lucas42 raised this 2026-06-09; folded into ADR §3/§4 + consequence, ticket #4.)
- **Stack = Go** (ADR §0; minimal attack surface, mature OIDC/WebAuthn libs, aligns w/ creds; alt=Node/node-oidc-provider; architect-decided, override invited). Domain **`aithne.l42.eu`** (confirmed). **Scope vocabulary = NEW dedicated repo `lucos_auth_scopes`** (confirmed name), YAML **published as a docker image, consumed at BUILD-TIME** by aithne + creds (no runtime polling/caching) — his preference over my configy-runtime idea; gets semver + own approval gate free. creds' freetext linked-cred scopes align to it. Cost: vocab change needs rebuild+redeploy (accepted).
- **M2M-credential-type convergence PARKED** (would bloat migration) — but keep door open (shared vocab + creds-held key + common JWT format).
- **Repo settings (sysadmin applies):** lucos_aithne `unsupervisedAgentCode=true` + lucos-security review required on all PRs; scope-registry repo `unsupervisedAgentCode=false`.

**Artifacts (2026-06-09):** ADR-0001 = **MERGED + Accepted** (PR #2, squash sha fcc0778; lucas42 approved text 2026-06-09; merged manually — aithne not in configy so no auto-merge workflow). Tickets: build = lucos_aithne#3 scaffold(Ready)/#4 store/#5 session-spine/#6 passkey/#7 OIDC/#8 machine-path/#9 scope-grant/#10 enrolment; lucos_aithne#11 token-brokering-audit(Ready); lucos_aithne#12 migrate-consumers(tracking, Blocked on parity); lucos#236 create scope-registry repo(Ready); lucos_creds#375 align scopes; lucos_configy#224 "whose-approval-to-auto-merge" config (his "consider", not directive).

**Consumer migration scoping (#12, 2026-06-16, my comment issuecomment-4716920756 — held for lucas42 review, NO sub-tickets raised yet):** Authoritative estate sweep (fresh origin/main local grep + direct code-read of each; code-search was lossy, see [[reference_github_codesearch_lossy_for_sweeps]]) → **exactly 11 identity consumers** (class-1, `/data?token` introspect + `/authenticate` redirect): notes, media_seinn, arachne(explore, `/explore` prefix), creds, loganne, media_metadata_manager, comhra, backups, photos, eolas, contacts. **Excluded (verified):** media_metadata_api (Bearer-key/creds only, no session), media_weightings (test-only ref), navbar-only static frontends (root/monitoring/scenes/mail/time). **5 share a copy-pasted Express `auth.js`** (arachne/creds/loganne/notes/seinn) → migrate one, other 4 are the same diff. Per-consumer change vs deployed #5 contract: drop `/data?token`→verify `aithne_session` JWT locally vs `aithne.l42.eu/.well-known/jwks.json`; redirect→`/auth/login?next=`; retire per-service `auth_token`/`token` cookie. **Untouched:** Bearer/CLIENT_KEYS machine paths in loganne/photos/eolas (creds, not aithne). **Canary = lucos_arachne** (lucas42 changed it from my `notes` pick, 2026-06-16, comment issuecomment-4717038765; ticket **lucas42/lucos_arachne#636 raised**, only the canary). Reason: arachne exercises BOTH principal classes — human `/explore` (the auth.l42.eu session consumer) AND agent `/mcp`. **Key finding:** arachne `/mcp` (`mcp/server.py` BearerAuthMiddleware) authenticates agents via `CLIENT_KEYS` Bearer — it NEVER used lucos_authentication, so migrating it is **net-new adoption of aithne's agent principal** (NOT decommissioning). aithne's agent flow IS built (lucos_aithne#8/ADR §5): `POST /oauth2/token` grant_type=client_credentials (client_id=agent slug, client_secret=machine key via `/admin/machine-keys`) → agent-class JWT → same JWKS. Canary covers both surfaces: explore→aithne_session JWKS; mcp→aithne agent-JWT JWKS w/ dual-accept (fall back to CLIENT_KEYS, drop fallback follow-up). Only the human surface counts toward the decommission gate. **Wave 0=arachne**; W1=notes/seinn/loganne(session); W2=mmm/comhra/photos; W3=eolas; W4(last)=backups/contacts/creds. Decommission preconditions: all 11 off + verify navbar asset not served from auth.l42.eu (aithne now serves `/lucos_navbar.js`). **Reusable: when picking/scoping a canary, check whether it has auth surfaces BEYOND the one your sweep matched — verify the consumer's full attack surface, not just the marker hit.** **Agent-credential home (lucas42, 2026-06-16, #636):** an agent-operating machine key (client_secret) lives in **lucos_agent/development env in lucos_creds (developer-writable)** — NOT prod creds. **aithne `/admin/machine-keys` is `aithne:admin`-gated + HTTP-ONLY (no CLI/host-exec bypass; only binary subcommands = `--healthcheck`, `--bootstrap-invite`-for-first-passkey).** So provisioning a PROD machine key needs lucas42's prod aithne-admin login (he runs the call, hands over the one-time secret). **CORRECTED 2026-06-16 (verified machine_credentials.go + grant.go + PR arachne#637): aithne does NOT default-deny at token ISSUANCE** — `/oauth2/token` authenticates on the machine key only; `GetActiveScopes` returns an EMPTY slice (not error) for a zero-grant principal; MintSession mints a valid JWT with empty `scopes` claim. **Default-deny is enforced at the resource/authZ layer, NOT at issuance.** arachne #637 /mcp gates on `principal_class=="agent"` alone (ignores scopes); explore accepts any valid aithne_session (no scope). So the canary's coarse cutover = **ONE prod aithne-admin action (provision machine key), ZERO scope grants, ZERO creds-writes**. A `/admin/grants` grant is only needed LATER if/when /mcp is tightened to check a real scope. **Lesson: don't carry a generic "default-deny ⇒ needs a grant to get a token" assumption — verify the issuance path; authN-easy/authZ-hard means issuance is authN-only.** **BUT §6-CONFORMANCE SUPERSEDES the "one action" conclusion (lucas42 flagged, 2026-06-16, comment 4717967195):** aithne minting zero-grant tokens + arachne#637 /mcp gating on `principal_class=="agent"` alone is exactly the **§6 VIOLATION**, not a feature. ADR-0001 §6: scope is "the ONLY safe lever for non-human principals"; "a sensitive action must never be gated on bare 'is there a valid session?' — a machine principal would pass"; fresh session "can do almost nothing until granted one named scope at a time". principal_class is a session property, NOT a granted capability. My "coarse scope to start" MISREAD §6 — its only "coarse" = a coarse GRANTED+CHECKED scope (`render-ui`), never absence-of-check. **Fix:** add `arachne:read` to lucos_auth_scopes (lucas42 approves per §7; supervised repo→normal PR) → rebuild/redeploy aithne (vocab build-embed; scope must exist before grantable) → /mcp middleware checks `arachne:read` in `scopes` claim (per-backend ENFORCEMENT) → lucas42 grants it via /admin/grants. **Cutover = TWO prod aithne-admin actions (provision key + grant scope), still zero creds-writes.** **Same hole on the human side:** #637 /explore accepts ANY valid aithne_session (no scope, no principal_class) → zero-scope agent gets UI free; fix=render-ui for non-human; whether humans also need a scope (uniform) is lucas42's policy call (estate-wide grant implications). #636 auto-closed on #637 merge but NOT validated → reopen. **META-LESSON: when a checked invariant (here ADR §6) governs a design, read it verbatim BEFORE concluding — a behaviourally-correct finding ("aithne issues the token") can still license a conformance-wrong recommendation.** **UNIFORM UI SCOPE-GATING = §6 CONFORMANCE, NOT a new policy (lucas42 corrected my framing, 2026-06-16, arachne#636 / #12):** §6 ALREADY mandates it — "a fresh session resolves to a near-empty default-deny scope … can do almost nothing until granted one named scope at a time" is EVERY session, humans included. The contact-id/identity lever is confined to "per-resource ownership aithne need not know about" (is this YOUR note?) layered ON TOP of access — NOT a scope bypass for service access like /explore. My "humans on identity, no scope" alternative had NO ADR basis — don't repeat it. **NO §6 amendment needed for the human-scope point** (ADR already says it). So: every service's authenticated UI is gated on a GRANTED scope for ALL principals — humans AND agents. Estate-wide: every #12 consumer gains a per-service read/UI scope + per-human grant (~8 new vocab scopes; eolas:read/media-metadata:read already exist & double for UI). Proposed `arachne:read` gates BOTH /explore + /mcp (capability not interface). render-ui = dev-only cross-service escape hatch (`scope OR (env==dev AND render-ui)`). **NO EXEMPTION / NO DEADLOCK (lucas42 corrected me twice, 2026-06-16 — final):** aithne's admin surface is NOT a carve-out from "every authenticated UI is scope-gated" — `aithne:admin` IS a scope, so gating admin on it is the admin surface FOLLOWING the rule (the one surface already obeying it). **Do NOT special-case aithne admin / skip its scope check — that's a hole.** What looked like a deadlock was only the bootstrap-SEED question (how the FIRST aithne:admin holder gets it without the admin surface) — already solved by **ADR-0002** `bootstrapAdmin`/`--bootstrap-invite` seeding the root grant OUTSIDE the admin HTTP surface (credential-gated: no-op once a passkey exists; re-seeds if all creds gone = host-level catastrophic recovery). §6+ADR-0002 are COMPLETE — nothing under-specified. login/enrol/jwks/_info are simply UNAUTHENTICATED (pre-auth/public), not exempt. `aithne:admin` = root-of-trust grant, seeded by bootstrap, all other scopes flow from it; only genuine lockout = losing aithne:admin (ADR-0002 host recovery). lucas42 = sole aithne:admin. **LESSON: don't dress a one-time SEED mechanism up as a runtime EXEMPTION — and don't invent "invariants/carve-outs" the spec doesn't need; I'd verified the bootstrap seed myself yet still mis-framed it as a deadlock.** **HOLISTIC VOCAB PASS for the remaining 10 (lucas42 decided, 2026-06-16, #12 comment 4718753508 — POST-CANARY, no action until arachne validates):** after the canary, design + add ALL 10 remaining consumers' scopes to lucos_auth_scopes in ONE estate-wide vocab PR + ONE aithne redeploy (not per-system). Front-loaded as a single scope-DESIGN pass BEFORE wave 1's first code migration; per-consumer CODE work still proceeds in the existing waves. Reasons: (1) collapse ~10 publish/bump cycles into 1; (2) force a holistic estate scope-set view (granularity, read/write splits, cross-cutting scopes) vs per-system-in-isolation. arachne keeps its merged `arachne:read`. Default-deny preserved (every batched scope maps to a real migrating consumer). **I (architect) fold this holistic scope-design pass into the #12 wave plan when I next revise it post-canary.** **Agent aithne-credential naming (set 2026-06-16, #636 runbook):** an agent's aithne OAuth2 machine key (client_secret) is stored as **`AITHNE_CLIENT_SECRET`** in lucos_agent/development creds — NOT `KEY_LUCOS_AITHNE` (in this estate `KEY_LUCOS_<SYSTEM>` = a CLIENT_KEYS-style DIRECT bearer key presented as `Authorization: key/bearer`; the aithne secret is an OAuth2 client_secret EXCHANGED at /oauth2/token for a JWT — different usage). Companions non-sensitive: client_id=agent slug, token base=`LUCOS_AITHNE_ORIGIN`. This is the template for all future agent principals. **aithne admin-API gap (raised lucas42/lucos_aithne#118):** `/admin/machine-keys` returns client_id/secret/credential_id but NOT principal_id; `/admin/grants` requires the internal principal_id (no external-id path; human UI resolves contact-id only). Fix (a) one-line: return principal_id (handler already holds it, machine_credentials.go:250) — fold into the canary vocab redeploy; (b) follow-up: grant-by-{class,external_id} before holistic pass (store already has GetPrincipalByExternalID(PrincipalClassAgent,slug)). **Agent admin UI design (lucas42/lucos_aithne#120, my design comment 4719016226, 2026-06-16):** new `/admin/agents` page (provision machine key + grant agent a scope) that REUSES the existing admin-grants pattern, NOT a parallel surface. Key reused mechanisms (from admin_grants.html/handleGrants): content-negotiated route (cookie-GET→HTML via requireAdminScopeFromCookie; Bearer→JSON via requireAdminScope); **`SessionToken` raw-JWT injected into page → JS sends `Authorization: Bearer` for mutations = the CSRF defence** (HttpOnly cookie alone can't mutate); CSP nonce; attribution from JWT claims. **Grant action REUSES `/admin/grants` POST unchanged** (no dup grant logic). Agent-specific = slug picker + machine-key provisioning only. **One-time client_secret is structurally non-re-displayable** (aithne stores only SHA-256 hash); UI shows once from POST response (no URL/history/log), copy + "store as AITHNE_CLIENT_SECRET now" warning. **#118 DECOUPLED from #120 (I pushed back on team-lead's "118b is prerequisite"):** agent page resolves slug→principal_id SERVER-SIDE at render (mirrors adminGrantsPage:502; store has ListPrincipals(Agent)/GetPrincipalByExternalID(Agent,slug)/ListCredentialsByPrincipal/ListGrants — ALL exist) → operator picks by slug, never pastes UUID, NO grant-API change → #118b is decoupled API-ergonomics, NOT a UI prereq; #118a (principal_id in machine-keys resp) nice-to-have. #120's real prereqs = its own adminAgentsPage handler + GET /admin/agents list endpoint (both on existing store methods). **Deliverable seam:** UX=templates/admin_agents.html (markup+JS+one-time-secret UX); developer=Go handler + /admin/agents list + #118a, lucos-security review; shared data contract. **Machine-key REVOCATION now IN #120 MVP (lucas42, 2026-06-16) — not fast-follow.** Verified credential model: `credentials` table = (id,principal_id,type,data,label,created_at) — **NO revoked_at** (only `grants` have it); `/oauth2/token` (handleClientCredentialsGrant) matches hash with NO revoked check; only `DeleteCredential` exists = HARD delete, tests-only caller. **Designed soft-revoke (mirror grant model, NOT hard delete — audit+consistency):** add revoked_by/revoked_at to credentials + RevokeCredential(id,revokedBy) + **SECURITY-CRITICAL: /oauth2/token hash-match must skip revoked (`&& c.RevokedAt==nil`) else revocation is COSMETIC** + new route `DELETE /admin/machine-keys/{id}` (mirror handleGrantByID). UI = key list w/ active/revoked state + Revoke button (JWT-Bearer DELETE + CONFIRM step, higher-stakes). Shared-schema note: revoked_at col also touches webauthn type → security review confirms bootstrapAdmin/login treat it consistently. #120 gates canary agent cutover; rides the same aithne redeploy as the arachne:read vocab embed. **Cutover ordering (hard):** vocab PR→aithne rebuild/redeploy→GRANT→then consumer enforces (grant-before-enforce). **§6 amendment WITHDRAWN** (human-scope already in ADR). Only genuine §6 under-spec = the grant-authority EXEMPTION isn't explicit (§6 says "every UI scope-gated" but doesn't carve out that aithne's admin surface stays reachable on aithne:admin alone) — offered as OPTIONAL one-line clarification, lucas42's call, not pushing it. **aithne session/access JWT contract (token/token.go origin/main):** `aud`=FIXED `"l42.eu"` for ALL principals (human session + agent client-cred + OIDC access) → consumers validate `aud=="l42.eu"` NOT their own origin; claims `principal_class` (`"human"`/`"agent"`) + `scopes` (plural array); iss=aithne issuer; MintSession TTL short. JWKS=`aithne.l42.eu/.well-known/jwks.json`. Agent JWT via `POST /oauth2/token` grant_type=client_credentials.

**Current `lucos_authentication` (the thing being replaced) — facts grounding the migration:** Node.js (single server.js). Does TWO bundled jobs: (a) identity via **third-party** OAuth providers (Google etc., providers.json) → maps provider identity to a lucos_contacts agentid via contacts `/identify`; (b) **third-party token brokering** (`/apptoken`, trusted `/data` w/ appkeys.conf) so apps call Google APIs. Consumers validate via `GET /data?token=` introspection. Also serves a shared `lucos_navbar.js`. Blast radius ≈12 consumer repos across 6+ languages (sandbox grep — authoritative list needs full estate sweep).

---

## Machine-principal sessions (the prior, already-agreed core)

Design agreed with lucas42 2026-06-01 while framing how lucos_root#135 (live homepage dashboard) depends on the new auth service. Tickets are Ideation — this is forward design, not yet built.

**Decision:** the new auth service should let a **non-human principal** (a service like lucos_root, or a trusted LLM agent) obtain a session **non-interactively** (pre-shared key, not a passkey ceremony), and backends validate that session **identically to a human session** (same `/data`-style lookup, resolving to a service-account contact ID).

**Why:** the alternative — a parallel bearer scheme — forces **dual authentication** on every backend (each grows a second auth path), because the estate's existing SSE / web-component endpoints are all built around the human session. Making machine principals session-capable means backends need **zero auth change**: lucos_root presents the session as a cookie (it's a server-side client, can set any header) and consumes existing human-session SSE feeds unchanged. Separate *acquisition* (non-interactive) from *presentation* (ordinary session) — the earlier wish-list discussion conflated them.

**Unification:** "lucos_root reads estate data as a service" and "trusted LLM agent authenticates" are the same requirement — a non-human principal needs a session. Solve once.

**Critical caveat — unify authN, NOT authZ:** a machine session validates like a human session but must NOT *be able to do* everything a human can. Any backend gating a sensitive action on "is there a valid session?" (not on contact ID) would start accepting machine principals. Two guard rails, decided in #132 not per-backend: (1) machine sessions scoped least-privilege; (2) session-validation response distinguishes principal class (human / lucos_root / agent) via contact ID or principal-type claim, for per-backend differentiation + clean audit.

**Lifecycle:** the non-interactive acquisition key mints sessions → crown-jewel secret → lucos_creds, per-env, rotatable. Larger blast radius than a normal service key (reinforces least-privilege scoping).

**Acquisition mechanism (resolved with lucas42 2026-06-02, #132):** lucos_creds is the home for the *acquisition key* — and it already does this (CI/CD deploy, agents via lucos_agents entry), so no new mechanism. lucas42's caveat "creds is deploy-time-pull-only, so unsuitable for short-lived tokens" is correct but lands on the *session* layer, which we deliberately keep OUT of creds: creds stores the long-lived key (deploy-injected into the principal's env), the short-lived session is minted at runtime by exchanging that key against the auth service, never stored in creds, never pulled at deploy. So no runtime creds dependency anywhere on the hot path — and that local-key-holding is what makes dropped/expired SSE streams cheap to re-mint+reconnect. Shape = OAuth2 client-credentials grant (key ≈ client credential, session ≈ access token), which satisfies the "standard protocol" wish-list item. Validator side symmetric: auth service holds a CLIENT_KEYS-style registry of valid keys, also creds-provisioned at its deploy. Rotation = same drill as Bearer migration (dual-accept window, takes effect next deploy).

**How to apply:** when #132 is designed/implemented, hold the line that the machine path yields a *session*, not a bearer scheme, and that authZ stays separate from authN. This is what makes lucos_root#135 cheap (no backend touches its auth). See also [[reference_service_worker_ui_as_system_component]] for the dashboard-compositor shape.

**Trusted-header REJECTED — sessions travel signed end-to-end (resolved with lucas42 2026-06-04, #132):** lucas42 asked whether the firewall+forward-auth+trusted-header model gives anyone with host SSH access carte-blanche (forge `X-Lucos-Contact-Id` on a direct-to-backend-port request, bypassing router). Answer: yes for the *naive plaintext-header* variant — and that's unacceptable because lucOS deliberately puts semi-trusted agents on hosts. Firewall closes the public-internet path to `http_port`, NOT the on-host/loopback/docker-bridge path. **Fix: the identity assertion handed to the backend is the signed session token (JWKS), verified locally — NOT a plaintext trusted header.** Forging identity then needs the signing key, not a network position → host access alone can't impersonate. This supersedes my earlier "contact ID handed down as a trusted header" line — strike it. Router still does redirect+rate-limit+edge; cost is each backend does one cheap local signature check (not zero auth). Honest residual limit: root-on-host still owns *that host's own* service (env/db/acquisition-key) — auth can't fix host compromise; what we preserve is the boundary that scoped host-access to system A ≠ identity-forgery against B/C/D. Also protects the authZ scoping: signed+scoped session means an agent can't forge a broader scope than it was minted with.

**Dev-only agent authZ to restricted pages (resolved with lucas42 2026-06-04, #132):** authZ half of the machine-principal design. lucas42 wants agents (esp. lucos-ux) to view rendered HTML/forms in dev they can't see in prod. Design: (1) Prod exclusion is FREE — agent creds are dev-env-only ([[creds-store-enumeration]]), so no prod acquisition key exists to mint a prod session. (2) Env-variation lives in the auth service (already per-env deployed): dev session carries coarse `render-ui` claim; backend stays dumb+uniform (checks the claim, never branches on ENVIRONMENT or "agent"). (3) **Scope to rendering (GET), NOT submission (POST/mutation)** — snapshotting needs only GET; agent can screenshot the delete-contact form but not POST it. (4) Start coarse (one claim), no per-form ACLs until proven needed. Invariant: safe only while dev-data is non-sensitive AND dev creds can't mint prod sessions.

**authN-easy / authZ-blocker (lucas42 corrected me 2026-06-08, #132):** I'd let the human gate drift onto credential *acquisition* ("only lucas42 writes prod creds, so a prod agent credential existing at all is a deliberate human act"). lucas42 pushed back: that over-weights controls around *acquiring* a credential vs what it can *do*. His line: **"authentication should be easy; authorisation should be the real blocker."** Folded in (and it simplifies): (1) ONE uniform easy acquisition mechanism across all envs — drop "dev is the only place an agent can authenticate" entirely, even as a default; (2) a fresh prod session resolves to a **near-empty default-deny scope** — authenticates fine, does almost nothing until granted; capability added one narrow named scope at a time; (3) the **crown jewel moves from the credential to the scope-GRANT** — the deliberate human-gated, auditable artefact is now "principal P may do X in env E", and #132 needs a scope-grant authority (default-deny, attributable); (4) **easy ≠ anonymous/shared** — each principal keeps its own distinct identity (principal-class+id in the session) or per-principal scope + audit collapse; (5) signed+scoped JWKS session makes granted scope unforgeable = what enforces "easy authN, hard authZ". **Honest cost:** no acquisition backstop anymore — default-deny + grant-registry discipline is load-bearing on its own. Strike my "prod agent credential existing at all is a human action" line.

**Centralised rate-limiting (resolved with lucas42 2026-06-03, #132):** lucas42 asked whether per-service rate-limiting (CodeQL/security flagging brute-force/resource-exhaustion on each backend's auth integration) can be centralised by redirecting unauthenticated users to a central system. Answer: decompose into (1) login ceremony — auth service only; (2) per-request session validation — today each backend calls `/data?token=` per request = amplification + the "validate credential against store" pattern CodeQL flags; (3) generic volumetric DoS. **The "by necessity each backend must check" pessimism is true ONLY in today's topology (open ports = central redirect is theatre, attacker hits port directly).** The firewall (ADR-0007, priority #1) inverts this: once enforcing, backend `http_port`s leave `public_ports` → reachable only via router → forward-auth at the router (nginx `auth_request`) becomes both possible AND safe, centralising redirect+check+rate-limit. **Footgun: trusted-header (`X-Lucos-Contact-Id`) injection is an estate-wide auth bypass if built before firewall enforce — strictly downstream of enforce.** Independent now-win regardless of firewall/gateway: **locally-verifiable signed sessions (JWKS)** — backend verifies signature locally, no outbound `/data` call → kills amplification + brute-force framing; same token the OAuth2 client-credentials machine path issues. Login/mint rate-limiting can never push down but lives on one system (auth service) by construction; volumetric DoS = router `limit_req` / Layer-3 (deferred in ADR-0007).
